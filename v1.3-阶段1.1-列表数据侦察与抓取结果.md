# v1.3 阶段 1.1 交付：国际站商机沟通列表数据侦察与抓取结果

> **日期**：2026-09-17
> **执行范围**：仅列表数据层（阶段 1.1）。未开发 AI Analyzer / cycle_engine / 新 wake_rules / 报告系统 / 飞书微信分发 / 自动发送 / 报价系统 / state 正式迁移 / 完整增量扫描。
> **数据纪律**：全程只读；真实抓取产物仅存 `captures/v1.3/`（已 gitignore）；生产 `state/`（opportunities / tasks / wakes / checkpoints）零改动。
> **证据文件**：`captures/v1.3/list_raw.json`（接口原始 20 行 × 58 字段 + DOM 对照 20 行）、`captures/v1.3/field_validation.json`（8 项验证 + 专项）、`captures/v1.3/list_normalized.json`（三段式标准化 20 行）。

---

## 0. 一页结论

| 项 | 结论 |
| --- | --- |
| 列表数据来源 | **XHR 优先达成**：`POST /message/ajax/feedback/subjectList.htm`（主列表接口），DOM 仅作对照（20/20 一致） |
| buyer_id | **`sender.accountId`（客户级稳定，verified）**：两次扫描一致；Buyer_A 三条询盘同值 `10000000001`；Buyer_B 三会话同值 `10000000002` |
| conversation_id | **`secTradeId` 不可用（每次响应重签，实测两次全变）**；稳定会话键改用 `tradeId` 本身；真正会话键待阶段 1.3 详情页确认 |
| Marina update_time | **`lastestReplyTime` = 最后活动时间（含已读动作），非最后消息时间**；禁止推导沉默天数（冻结规则维持）。指定线索 Buyer_O（13900000015）在本账号列表与搜索中均未出现（搜索 totalCount=0），以替代证据 + 平台机制结论 |
| 已读/未读 | **结构存在且可抓（hasUnread / unreadCount / readTime / latestMessage.contactRead）**，但当前账号无未读样本（totalCount=0），映射未实测 → probable |
| 30 天窗口 | **平台不提供时间筛选**：须客户端按 `lastestReplyTime`（或详情层 `latestMessage.gmtChat`）过滤；totalCount=1892 / totalPage=95 / pageSize=20，无扫描上限 |
| 数据安全 | **发现既有泄露**：仓库为 Public，11 个真实客户数据文件已推送至 GitHub；已本地隔离（gitignore + 索引移除），**远程历史清理待老板决策** |

---

## 1. 实际访问了哪些国际站页面

| 页面 | URL | 结果 |
| --- | --- | --- |
| 商机沟通列表 | `https://message.alibaba.com/message/default.htm`（`https://message.alibaba.com/` 跳转至此；`/message/` 直接访问 404） | 登录态有效；`#feedback/all/` 视图；page1 由 SSR 全局 `window.feedbackListResponse` 提供，page2/3 通过真实点击分页控件触发 XHR |
| 列表搜索（查询用） | 同页搜索框，服务端 `params.search.senderName` | 已验证可用（Marina 专项、Buyer_A 定位） |
| UNREAD 视图（导航） | `#feedback/UNREAD` | totalCount=0，账号当前无未读 |
| TM 消息管理器（未重新访问） | `https://onetalk.alibaba.com/message/messageManager.htm` | 未进入（本轮只做列表层，TM 渠道留待阶段 1.2/1.3） |
| 会话详情页（未进入） | `maDetail.htm?imInquiryId=...` | **刻意未进入**——点击列表行会标记已读，违反只读纪律；详情层属阶段 1.3 |

全程未发送消息、未改分配、未标记已读、未修改任何后台数据；仅翻页 / 切视图 / 搜索查询。

## 2. 列表数据来自 DOM 还是 XHR

**XHR（主）。** 主接口：

```
POST /message/ajax/feedback/subjectList.htm?ctoken=<token>&dmtrack_pageid=<pageid>
Content-Type: application/x-www-form-urlencoded
_csrf_token_=<csrf>&postId=<epochMs>&params={"system":"feedback","listType":"all","pagination":{"nextPage":"2","pageSize":20}}
```

响应：`{code:200, data:{list:[[row]×20], pagination:{totalCount:1892, totalPage:95}, ...}}`。

辅助接口：`subjectListExtraInfo.htm`（page≥2 行的 latestMessage 等消息级字段补充）、`unread.htm`（导航未读徽标）、`queryFolders.htm`（文件夹树）。

DOM 作为对照集：20 行与 XHR **完全 1:1**（顺序、ID 集合、owner/buyer/country 全一致，交叉验证键 = tradeId）。page1 无需发请求（SSR 全局变量直接可读）。

## 3. 原始接口/页面字段有哪些

page1 SSR 每行 **58 字段**（page≥2 每行 35 字段，缺 latestMessage/imContent/tags，须补调 `subjectListExtraInfo`）。核心字段（全字段清单见 list_raw.json）：

| 类别 | 字段 |
| --- | --- |
| 标识 | `tradeId`（==feedbackId==requestNo==id）`secTradeId`（重签令牌）`secFeedbackId` `messageId` `requestNo` `imInquiry` |
| 买家 | `sender.{accountId, contactId, vaccountId, secAccountId, secContactId, alitalkId, chatToken, name, companyName, countryName, countryCode, joinYear, isContact, userNewLevel, highQuality}` |
| 归属 | `ownerName` `ownerId` `secOwnerId`（重签令牌） |
| 时间 | `lastestReplyTime` `modifiedTime` `readTime` `createTime` `latestMessage.{gmtChat, gmtChatLong, revertTime}` `lastTime`（相对秒，可为负） |
| 消息 | `imContent` `latestMessage.{content, messageId, sendType, imMessageType, uuid, viewType, contactRead}` `messageType`（REC/SEN）`replyStatus` |
| 已读未读 | `hasUnread` `unreadCount` `moreUnreadCount` `readTime` `mainShowRead` `subShowRead` `latestMessage.contactRead` |
| 状态/来源 | `process`（洽谈中…）`source` `appFrom` `subject` `feedbackType`（1=询盘/RFQ含productInfo，6=TM聊天）`mark` `originProcess` `fillingPercent` `targetIds` `productInfo[]` |

## 4. buyer_id 候选是什么，可靠性如何

| 候选 | 实测 | 结论 |
| --- | --- | --- |
| **`sender.accountId`（主候选）** | 两次扫描（SSR×2 + XHR×2，间隔约 2.5 分钟）tradeId→accountId 完全一致；**跨 inquiry**：Buyer_A 三条询盘（13900000008 / 13900000006 / 13900000007）accountId 均为 `10000000001`，且 owner 均 Alice Lam、country 均 Rwanda；旁证 Buyer_B 三会话（13900000012 / 13900000013 / 13900000014）均 `10000000002` | **verified，客户级稳定，可作 buyer_id**。且它是纯数字账号 ID（非聊天联系人/临时会话标识）——三询盘同值证明其代表买家本人账号，而非会话 |
| `sender.login_id` / `loginId` | 列表接口**未出现**（仅 TM messageManager 联系人接口有，旧 captures 样本） | 列表层不可用；TM 渠道与 accountId 的对应关系留待阶段 1.2 验证 |
| `sender.contactId` | 结构存在、值稳定形态待定 | 备选（会话联系人级），不采用 |
| `sender.vaccountId` / `secAccountId` / `secContactId` / `alitalkId` / `chatToken` | vaccountId 数字、sec 系列为哈希/加密值 | 不采用（虚拟账号/哈希，语义不明） |
| `sender.name` | 与 DOM 一致 | 展示用，不作标识（规格 §4.1 已禁止姓名冒充） |

未因字段名像 buyer_id 就直接认定——认定依据是**跨 inquiry 一致性实测**（两个买家共 6 条询盘）。

## 5. conversation_id 候选是什么

- **列表不直接提供稳定 conversation_id**。`secTradeId` 形似会话 ID（= 详情 URL 哈希段 = data-ids 第 3 段），但实测**每次响应重新签发**：同一 tradeId 两次抓取，secTradeId 全变（样本 6 条，见 field_validation v2）；同一渲染内 DOM 哈希段 == 同响应 secTradeId（说明它只是本轮会话令牌）。
- **处理**：`conversation_id` 标准化输出恒 null，置信 `unstable`，禁止落库；`secOwnerId` 同性质一并排除。
- **当前会话级稳定键 = `tradeId`**（询盘号，与 conversation 一一对应：DOM 20/20、翻页 60 行无重复）。TM/商机沟通是否共用 ID 体系、详情页是否存在真会话键——留待阶段 1.3 详情层调查。

## 6. 已读/未读如何取得

- **数据结构存在（XHR）**：`hasUnread`（bool）`unreadCount`（数字）`moreUnreadCount` `readTime`（epoch ms）`mainShowRead`/`subShowRead` `latestMessage.contactRead`。
- **本账号当前无未读会话**：UNREAD 视图 totalCount=0；60 行 hasUnread 全 false / unreadCount 全 0；`unread.htm` 仅返回 spam 徽标 {spam:15}；DOM 无任何 unread/red/bold 样式。**因此 unread→DOM 徽章映射未实测（probable）**——不是“平台不提供”，而是本轮无样本。技术结论：字段可抓（结构与类型稳定），映射验证留待出现真实未读时补测。
- `contactRead`（page1 20 行 true=4 / false=16）推断为**买家已读回执**（我方最后消息对方是否已读），语义待阶段 1.3 详情层确认。
- 约束遵守：为避免点入详情标记已读，未读验证只从列表层取证。

## 7. last_message_time 是否存在

**存在（列表层即有）**：`latestMessage.gmtChatLong / gmtChat`（最后一条聊天消息时间，epoch ms，精确到毫秒）+ `imContent`（该消息文本）。标准化映射 `last_message_time = gmtChatLong`（20/20 填充，verified）。

配套 `latestMessage.sendType`（"rec"=买家发 / "send"=卖家发；messageType REC/SEN 同义）→ `last_message_sender`（probable：内容佐证 2/2 —— Alex 行 rec+买家提问文本、Buyer_D 行 send+卖家阿拉伯语推销文本，但样本少，方向语义待详情层消息级验证）。

**注意**：page≥2 的行无 latestMessage，须补调 `subjectListExtraInfo`（按 secTradeIds+tradeIds 批量取），否则丢最后消息字段。

## 8. Marina update_time 最终调查结论

- **列表“更新时间”= `lastestReplyTime` = `modifiedTime` = `readTime`（三者同值，DOM 20/20 吻合；今日消息显示 HH:MM、历史显示 YYYY-M-D，旧脚本解析失败根因即此）**。
- **语义 = 最后活动时间，含已读动作，≠ 最后消息时间**：实证 13900000001 —— DOM 更新时间 17:53，但 `latestMessage.gmtChat` = 17:34（买家 17:34 提问，业务员 17:53 已读，readTime=17:53）。
- **v1.2 记录的“Buyer_O 列表 update_time=2025-09-08 vs 详情最后消息 2026-04-26”矛盾**：本轮无法复现——指定线索 Buyer_O（13900000015）在列表搜索（senderName=Marina / 关键词 13900000015）中 totalCount=0，**未能在本账号当前列表定位**（可能已归档/转移/属其他视图）。以替代证据收案：Buyer_I（100000000002）DOM 更新时间 2026-1-9 == lastestReplyTime == modifiedTime == readTime，且该行无 latestMessage（无消息亦有活动时间，进一步证明“活动时间”语义）。
- **冻结规则维持并升级**：`list_update_time` 仅作 CHANGED 初筛信号，**禁止用于推导客户沉默天数**；沉默判定必须用消息级 `gmtChat`（详情层，阶段 1.3）。

## 9. 30 天扫描如何实现

1. **平台无时间筛选**（列表页无时间控件；subjectList 请求无任何时间参数；UNREAD 是视图非时间过滤）→ 30 天窗口必须**客户端过滤**：抓取行 → 按 `lastestReplyTime`（初筛）+ `latestMessage.gmtChat`（精确，需 ExtraInfo 补齐）≤ 30 天判定。
2. **扫描方式**：page1 SSR（免请求）→ page2..N 翻页 XHR。实测分页可达（点击翻页 + 接口参数 nextPage=2/4、pageSize=100 均 code:200）。
3. **注意**：直接 fetch 重放响应为 HTML 实体转义 JSON 且实体解码后仍难恢复合法 JSON（双转义 productInfo）；**正式采集器应走页面内 XHR 钩子/SSR 路径**，不走裸 fetch 重放。
4. 30 天窗口与重入集合并的完整增量扫描属阶段 1.5，本轮未开发。

## 10. 分页机制

| 项 | 实测 |
| --- | --- |
| 参数 | `params.pagination.{nextPage: <页号>, pageSize: 20}`（JSON 字符串塞在 form 字段 params 里），另有 `system=feedback` `listType=all` |
| 认证 | cookie + `_csrf_token_` + URL `ctoken` + `dmtrack_pageid`（均可从页面上下文获取） |
| 总量 | `pagination.totalCount=1892`，`totalPage=95` |
| 上限 | **无扫描上限**（page2/3 实抓 + nextPage=4、pageSize=100 参数级验证均成功） |
| 完整性 | page1+2+3 共 60 行，去重后 60（**零重复零漏行**）；page2 两次抓取顺序 20/20 一致 |
| 字段差异 | page1=58 字段/行（SSR），page≥2=35 字段/行 → 需补调 `subjectListExtraInfo` |
| 搜索 | `params.search.senderName`（服务端搜索，Marina/Buyer_A 专项已用） |

## 11. 哪些字段已确认（verified）

| 字段 | 源字段 | 证据 |
| --- | --- | --- |
| lead_id | tradeId | DOM/XHR/SSR/重复扫描全一致；20 行唯一 |
| buyer_id | sender.accountId | 两次扫描一致 + 跨 inquiry 一致（Buyer_A×3 同值 10000000001） |
| buyer_name | sender.name | DOM 20/20 |
| country / country_code | sender.countryName / countryCode | DOM 国旗 title 前缀 20/20（缺失行如实 null，如 13900000005 平台侧即缺） |
| owner | ownerName（+ownerId 唯一映射） | DOM 60/60；Alice Lam=20000000001 / Ella Chen=20000000002 / Bin Cai=20000000003 |
| last_message_time | latestMessage.gmtChatLong | 结构稳定，20/20 填充 |
| list_update_time | lastestReplyTime | DOM 20/20；语义已定（最后活动时间） |
| inquiry_source / subject / feedback_type | source / subject / feedbackType | 结构稳定 |

多业务员识别：主账号视图覆盖 3 名业务员（Ella 31 / Bin 18 / Alice 11，60 行），owner 变更可检测。

## 12. 哪些字段仍然 unknown / unverified

| 字段 | 状态 | 原因与出路 |
| --- | --- | --- |
| conversation_id | **unstable**（恒 null） | secTradeId 重签，不可落库；待阶段 1.3 详情页找真会话键 |
| last_message_sender | probable | sendType 语义 2 样本佐证；详情层消息级 sender 验证后升 verified |
| read_status / unread_count | probable | 结构稳定但当前无未读样本；出现真实未读时补测 DOM 徽章映射 |
| company | probable（填充 12/20） | companyName 部分行缺失/等同人名；详情层验证 |
| contactRead 语义 | unverified | 推断买家已读回执，待详情层确认 |
| login_id ↔ accountId 对应 | unknown | TM 渠道接口未在本轮验证（阶段 1.2） |
| last_message_sender 的机器人/自动接待区分 | unknown | 列表层无此字段，详情层 imMessageType/viewType 调查（阶段 1.3） |

## 13. 抓取器修改了哪些文件

| 文件 | 变更 |
| --- | --- |
| `capture_list.py` | **新建**：列表数据标准化模块（raw/normalized/field_confidence 三段式；禁止猜测，未确认即 null/unverified；DOM 对照交叉验证；不写生产 state） |
| `captures/v1.3/list_raw.json` | 新建：接口原始 20 行 + DOM 对照 + 分页/时间筛选/未读调查记录（gitignored） |
| `captures/v1.3/field_validation.json` | 新建：8 项验证证据（gitignored） |
| `captures/v1.3/list_normalized.json` | 新建：标准化输出（gitignored，运行 `python capture_list.py` 可再生） |
| `captures/v1.3/_*.js` | 新建：浏览器侧 helper 脚本（gitignored，正式采集器可复用其逻辑） |
| `.gitignore` | 修改：新增 captures/ 整体隔离 + 既有抓取文件模式 |
| `v1.3-Accio-Work-询盘助手执行规格.md` | 修改：本轮开工前按老板审订完成三处修正（Wake 四态 / 三分类 / checkpoint run_type+run_status），修订记录见文档头 |
| Git 索引 | `git rm --cached`：9 个含真实客户数据的文件移出跟踪（本地文件保留）；另 3 个纯抓取脚本（`_alibaba_inquiry_extract.js` / `_alibaba_inquiry_times.js` / `extract_chat.js`，无客户数据）保留跟踪——见 §15 |
| 生产 state/ | **零改动** |

## 14. 测试结果

| 项 | 结果 |
| --- | --- |
| capture_list.py 标准化运行 | 20 行，lead_id 唯一 20/20，JSON 合法，UTF-8 中文完整 |
| DOM 对照交叉验证 | 20/20 匹配，0 不一致（owner/buyer_name/country） |
| buyer_id 重复扫描 | 两次（SSR×2+XHR×2）完全一致 |
| conversation_id 重复扫描 | 实测不一致（secTradeId 重签）→ 已按 unstable 处理 |
| 跨 inquiry buyer_id | Buyer_A×3 = 10000000001 ✅；Buyer_B×3 = 10000000002 ✅ |
| owner / country（XHR vs DOM） | 60/60 一致 / 59/60 一致（1 行两侧皆空，非冲突） |
| update_time（DOM vs XHR） | 20/20 相等（lastestReplyTime） |
| 分页完整性 | 60 行零重复零漏行；page2 两次顺序一致 |
| 遗留 v1.2 测试套件 | 未动（test_engine 61 项属 v1.2.1 冻结基线；capture_list.py 为独立调试工具未加正式测试，正式夹具待匿名化后建） |

## 15. 是否存在数据安全风险

**存在，且已发现既有泄露。**

1. **已发生（高危）**：仓库 `github.com/otaku-007/inquiry-tracking-assistant` 为 **Public**。已推送内容包括：
   - 9 个含真实客户数据的抓取文件：captures/ 6 个（真实买家姓名、login_id、25 条完整聊天、详情 URL 哈希）、`_alibaba_inquiry_page1*.json` 2 个（真实询盘列表）、`deal-assistant-13900000020-report.html` 1 个（真实客户报告）；
   - `state/` 10 个文件（含 backups）：40 条真实买家姓名 + lead_id + owner + 国家 + 意向标记（v1.2 基线提交时随仓库一起推送）；
   - email/phone 字段经查为 null（联系方式本体未泄露），但姓名/公司/国家/聊天内容已公开。
2. **已执行的本地防御**：`.gitignore` 隔离 captures/ 与抓取文件模式；`git rm --cached` 将 9 个含真实数据的抓取文件移出索引（本地文件保留，可随时恢复跟踪）；3 个纯脚本（无客户数据）未动。**尚未提交**——是否提交由老板决定。
3. **待老板决策的补救**（外部高影响操作，未经批准不执行）：
   - a. 将仓库转 Private（最快止血，GitHub 设置页一键）；
   - b. 提交移除索引的变更（新 commit 停止继续公开，但 git 历史仍含旧数据）；
   - c. `state/*.json`（含真实买家姓名，v1.2 基线提交时推送）是否同样移出索引 + gitignore——本轮未擅自改动（涉及 v1.2 冻结基线的仓库形态）；
   - d. 彻底清除需重写远程历史（filter-repo + force push）或删除重建仓库（远程历史改写不可逆）；
   - e. 正式测试夹具一律匿名化/合成数据（本轮已遵守：无真实数据进入任何将提交的文件）。
4. **建议顺序**：a（立即）→ b（本轮内）→ c/d（老板确认后）。

## 16. 下一阶段建议

按规格阶段 1 顺序（本轮已停在 1.1，未越界）：

1. **先处理数据安全**（§15 建议 a→b→c），再做任何含真实数据的新提交。
2. **阶段 1.2 buyer_id 调查收尾**：TM `login_id`（messageManager 联系人接口）↔ `accountId` 对应关系验证；buyer profile 页兜底路线。现有证据已强（accountId 客户级稳定），1.2 可轻量化。
3. **阶段 1.3 详情层**：`capture_detail.py`——消息级 message_id/时间/sender/kind（机器人 vs 人工可辨识字段，imMessageType/viewType 是线索）/文本；确认 contactRead 语义；寻找真会话键（若存在）；page≥2 行补调 `subjectListExtraInfo` 的集成。
4. **正式采集器设计要点（本轮侦察直接输入）**：page1 走 SSR 免请求；page≥2 走页面 XHR 钩子（不用裸 fetch 重放）；分页无上限但 30 天窗口客户端过滤可早停（列表按活动时间倒序，遇到 >30 天行即可停页）；owner 变更检测用 ownerId 映射表；unread 出现真实样本时补测映射。
5. **阶段 1.5 增量扫描 / 1.6 cycle_engine**：按修订后规格（§3.2 四态 WakeEvent、§5.3 run_type/run_status）实施，规格修订已完成、无需再议。

---

## 附：规格修订完成确认（本阶段前置工作）

开工前已按老板审订完成三处修正并写回 `v1.3-Accio-Work-询盘助手执行规格.md`（732→789 行）：

1. **Wake 执行语义四态化**：取消“报告出现行动卡=已执行”；`due / reported / sent / cancelled` 四态 + WakeEvent 预留 `due_at / reported_at / actual_sent_at / source_message_id / status` 五字段；下一轮 due_at 基于上一轮 actual_sent_at 计算；FINAL WAKE 以实际发送确认为周期完成判定（§3.1/§3.2/§12.3 已同步）。
2. **卖家主动消息三分类**：`followup_wake`（占当前轮次、actual_sent_at=真实消息时间）/ `substantive_update`（结束旧 waiting phase、重建锚点、重新从 3 天起算）/ `courtesy`（不动锚点不占轮次）；Analyzer 输出契约新增 `seller_message_class` 字段；仅定义数据结构与规格，未开发 AI（§3.3/§7.2）。
3. **checkpoint 运行状态**：`run_type = scheduled|catchup|manual` + `run_status = running|success|partial|failed`；仅当日存在 `run_status==success` 才视为已完成，partial/failed 允许补跑（§5.3）。

同步更新受影响联动点：§2.1 S11、§3.5 结束条件表（新增 seller_substantive_update）、§5.2 候选集、§11 D1（标记已解决）/D4（澄清）、§12.3/§12.4 验收标准、术语表。
