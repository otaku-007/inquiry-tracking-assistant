# 每日询盘跟进 v1.2.1 —— 交付与验证结果（冻结版）

> 版本基线：v1.2
> 本次范围：修 2 个隐藏问题 + 补集成测试（不新增功能）
> 测试结果：**61 项 PASS / 0 FAIL**（原 40 项 + 新增 21 项断言）
> 真实数据回归：**零行为变化**

---

## 一、修复的两个隐藏问题

### 问题 1：唤醒次数重复计数（off-by-one）

**位置**：`daily_run.py` `_record_wake_contacts()` 与 `wake_rules.py` `record_wake_contact()`

**成因**：调用顺序是「先追加、后计数」。

```
se.record_contact(wakes, lid, contact)   # 把本次联系追加进 w["contacts"]
wr.record_wake_contact(w, contact)       # 内部执行 cycle_wake_count(w) + 1
```

而 `cycle_wake_count()` 在缺少显式计数时会**退化为按 contacts 派生**：

```python
def cycle_wake_count(w):
    if "cycle_wake_count" in w:
        return int(w["cycle_wake_count"] or 0)
    return len(wake_contacts(w))   # ← 此时已包含刚追加的本次
```

于是首次真实唤醒变成 `contacts=1 → 派生=1 → +1 → 记为 2`。

**实测复现（修复前）**：

| 观察点 | 实测值 | 正确值 |
| --- | --- | --- |
| contacts 中 wake 条数 | 1 | 1 |
| cycle_wake_count | **2** | 1 |
| 第 2 次唤醒后 cycle_wake_count | **3** | 2 |
| 第 2 次唤醒后 cooldown_until | **2026-10-22** | None |

**影响**：不只是数字偏大。因为 `cwc >= cycle_max(3)` 时写冷却期，计数虚高会让**冷却期提前一次触发**——客户实际只被唤醒 2 次就被冻结 30 天。

**修复**：把「本次之前的次数」提到追加前取得，显式传入，使计数与调用顺序解耦。

```python
# daily_run._record_wake_contacts
prior = wr.cycle_wake_count(w)           # 必须在写入 contacts 之前取
_, added = se.record_contact(wakes, lid, contact)
if added and contact.get("kind") == "wake":
    wr.record_wake_contact(w, contact, prior_count=prior)

# wake_rules.record_wake_contact(w, contact, prior_count=None)
base = cycle_wake_count(w) if prior_count is None else int(prior_count)
cwc = base + 1
```

保留 `prior_count=None` 的默认分支，兼容「本次联系尚未写入 contacts」的纯函数式调用。

---

### 问题 2：buyer_id 客户级聚合查找错配

**位置**：`wake_rules.py` `aggregate_buyer_contacts()` 与 `generate_report.py` `_evaluate_wakes()`

**成因**：写入键与查询键不一致。

```python
# 写入侧（aggregate_buyer_contacts）：键 = buyer_id 优先
bid = o.get("buyer_id") or o.get("buyer")

# 查询侧（_evaluate_wakes）：却拿姓名去查
buyer = next((o.get("buyer") for o in opps if o.get("lead_id") == opp_key), None)
agg = buyer_agg.get(buyer)          # ← 有 buyer_id 时永远查不中
```

**实测复现（修复前）**：两条线索同一 `buyer_id=BID-1`，聚合联系人头 6 条（已达 90 天滚动上限）。

| 观察点 | 实测值 | 正确值 |
| --- | --- | --- |
| 聚合键 | `['BID-1']` | — |
| identity_verified | True | True |
| 聚合联系数 | 6 | 6 |
| 日报判定 | **可联系（×2），client_level_verified=False** | 等待至某日 |

**影响**：客户级 90 天滚动限次**永远不会生效**。`rolling_contacts` 恒为 `None`，`decide_wake()` 只能退回用单条线索的 contacts 计数，于是把已达上限的客户误判为「可联系」——正是限次规则要防的事。

**修复**：抽出统一的 `buyer_key()`，写入侧与查询侧共用，杜绝键漂移。

```python
def buyer_key(opp):
    """客户聚合键：优先稳定 buyer_id，缺省回退 buyer 姓名（不可信，仅作退化）。"""
    if not opp:
        return None
    return opp.get("buyer_id") or opp.get("buyer")

# aggregate_buyer_contacts 与 _evaluate_wakes 均改用它
# 顺带跳过既无 buyer_id 又无姓名的线索，避免污染聚合
```

---

## 二、新增集成测试（5 个函数 / 21 项断言）

此前 40 项测试主要直接调 `decide_wake()`，**没有覆盖「实际写入一次联系」的端到端链路**——这正是两个问题能藏住的原因。

新增测试在临时 `STATE_DIR` 上跑真实的 `daily_run.run_daily()`，直接验证落盘结果。

| 编号 | 测试函数 | 覆盖内容 |
| --- | --- | --- |
| 13a–13g | `test_integration_wake_count_once` | 首次唤醒计数=1；第 2 次=2 且不冷却；第 3 次=3 且冷却=+30 天 |
| 14a–14b | `test_integration_wake_rerun_idempotent` | 同批次重跑不新增联系、不增长计数 |
| 15a–15d | `test_integration_reply_resets_cycle` | 实质回复归零；历史联系保留；再唤醒从 1 重新开始 |
| 16a–16e | `test_integration_buyer_id_aggregation_used` | 聚合键为 buyer_id；客户级聚合真正参与判定；达上限不再判可联系 |
| 17a–17c | `test_integration_no_buyer_id_falls_back` | 无 buyer_id 时回退姓名、标记不可信、不误判超限 |

### 测试有效性证明（关键）

把 3 个修复文件回退到基线提交 `2bf53ce`、**保留新测试**后运行：

| 结果 | 数量 |
| --- | --- |
| PASS | 53 |
| **FAIL** | **8** |

失败的 8 项精确对应两个问题，说明这些测试是真正的回归护栏，而不是无论如何都通过的摆设：

```
[FAIL] 13b 首次唤醒周期计数=1        -> cycle_wake_count=2（旧实现为 2）
[FAIL] 13d 第2次后计数=2             -> 3
[FAIL] 13e 第2次后仍不冷却           -> cooldown_until=2026-10-22（旧实现会提前冷却）
[FAIL] 13f 第3次后计数=3             -> 4
[FAIL] 14b 重跑不增长周期计数        -> first=2 second=2
[FAIL] 15a 首轮计数=1                -> 2
[FAIL] 16c 客户级聚合已真正参与判定  -> [('L1', False), ('L2', False)]
[FAIL] 16d 达 90 天滚动上限不再判可联系 -> contactable=2 verify=0（旧实现误判为可联系）
```

修复后：**61 PASS / 0 FAIL**。

---

## 三、真实数据回归（零行为变化）

在真实 `state/` 上只读复核：

| 指标 | 修复前 | 修复后 |
| --- | --- | --- |
| 原始线索 | 40 | 40 |
| 独立会话 | 37 | 37 |
| 独立客户（按名） | 35 | 35 |
| 待办 | 9 | 9 |
| 历史候选·今天可联系 | 0 | 0 |
| 历史候选·待核实 | 4 | 4 |
| 状态读取错误 | — | 无 |

**为什么零变化**：

- 真实 `wakes` 中**没有任何** `cycle_wake_count` 字段，也没有 `kind=wake` 的联系记录 → 问题 1 的修复没有可重算的存量数据。
- 真实线索**全部没有 `buyer_id`** → 问题 2 的修复路径未被触发，仍走姓名回退。

也就是说：两个问题在**当前真实数据上尚未显形**，但一旦开始真实记录唤醒、或一旦拿到稳定 `buyer_id`，就会立刻造成错误判定。本次属**提前拆雷**。

---

## 四、v1.2 冻结声明

自本版本起，**v1.2 状态。**

| 项目 | 状态 |
| --- | --- |
| 状态持久化引擎（幂等合并/人工字段保护/跨日到期/损坏保护/联系去重） | ✅ 已冻结 |
| 统一唤醒资格判断（周期上限/工作日间隔/冷却/滚动限次/暂停/停止联系） | ✅ 已冻结 |
| 日报动态渲染（日期/范围/计数全部来自本次运行） | ✅ 已冻结 |
| 唤醒计数与客户级聚合的正确性 | ✅ 本次修复并加护栏 |
| 测试护栏 | ✅ 61 项（40 单元 + 21 集成） |

---

## 五、仍未解决（不属本次范围）

1. **客户级 90 天滚动限次仍未真实生效**——聚合逻辑已修好，但真实数据缺 `buyer_id`，`identity_verified` 仍为 False。需要抓取侧提供稳定买家标识，这条规则才会真正落地。
2. **无显式计数器时的派生语义仍有隐含依赖**——`cycle_wake_count()` 在缺字段时按 contacts 派生，而 contacts 是永久历史；目前靠 `apply_substantive_reply()` 写入显式 0 来表达「周期归零」。若未来新增重置路径而忘记写显式计数，会重新引入同类偏差。已通过 `prior_count` 消掉当前的坑，但更彻底的做法是引入显式的周期边界时间戳。
3. **抓取通道未替换**、**定时运行**、**按业务员分发**、**节假日历** 均未实现（见 v1.2 与方案文档）。

---

## 六、变更文件

| 文件 | 变更 |
| --- | --- |
| `wake_rules.py` | `record_wake_contact()` 增加 `prior_count` 参数；新增 `buyer_key()`；`aggregate_buyer_contacts()` 改用 `buyer_key()` 并跳过无键线索 |
| `daily_run.py` | `_record_wake_contacts()` 在写入前取 `prior` 并显式传入 |
| `generate_report.py` | `_evaluate_wakes()` 改用 `wr.buyer_key()` 查询聚合 |
| `test_engine.py` | 新增 5 个集成测试函数 / 21 项断言；更新模块说明 |
