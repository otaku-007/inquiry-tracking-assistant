# 询盘盯盘助手 —— 现状盘点、开发方案与待审核决策

> 生成时间：2026-09-17（北京时间）
> 依据：本文件夹内 7 份 md + 6 个 py 模块 + `state/` 实际数据，**本次已实测复验**
> 用途：**请你审核**。本文只做盘点与方案，**未改动任何代码、未抓取、未发送任何消息**

---

## 一、你的要求 → 拆成 5 个能力

| 你要的 | 对应能力 | 当前状态 |
| --- | --- | --- |
| 抓每天新增的询盘 | L1 列表抓取 | ⚠️ 通道未定 |
| 抓待激活客户的询盘 | L1 + 唤醒召回 | ⚠️ 跑通了，但**产不出行动** |
| 发给对应的业务员 | L4 分发 | ❌ 未开始 |
| 提醒联系谁 / 聊什么 / 做什么动作 | L3 语义层 + 动作卡 | ⚠️ 动作卡已有，语义靠模型临时读 |
| 消耗要低 | 分层设计 | ⚠️ 目前抓取靠 browser 子代理，token 未计量 |

---

## 二、当前进度（本次实测，不是转述文档）

### 2.1 已完成并复验通过

本次实跑 `python -X utf8 test_engine.py` → **40 项 PASS / 0 FAIL**。

| 模块 | 文件 | 作用 | 实测 |
| --- | --- | --- | --- |
| 状态引擎 | `state_engine.py` | 幂等合并、人工字段保护、跨日到期、损坏保护、联系去重 | 测试 1–10 全过 |
| 唤醒规则 | `wake_rules.py` | 统一资格判断：可联系／等待至某日／待核实／停止联系 | 测试 11a–11k 全过 |
| 约定暂停闭环 | — | 买家说"10 月 5 日再联系"→ 到期前等待、到期转复查、客户回复后重开 | 测试 12a–12c 全过 |
| 日报渲染 | `generate_report.py` | 日期／范围／计数全部来自本次运行，不写死 | — |
| 日常入口 | `daily_run.py` | 备份→安全读→合并→事件→刷新→检查点→出报 | — |
| 演示入口 | `daily_run.demo()` | 在临时副本跑通"新线索／旧会话新回复重开／约定暂停" | — |

### 2.2 真实状态（`state/` 当前实际内容，本次逐项核对）

- 线索 **40** ／ 独立会话 **37** ／ 独立客户（按名）**35**
- 任务 **12**（其中 open **9**）
- 待办分布：承诺待兑现 2、高意向待推进 2、重复线索核实 2、前置条件未满足 1、订单收尾待办 1、待核查 1
- 按人：Ella Chen **5**、Alice Lam **2**、Bin Cai **2**、**Alan Au 0**
- 唤醒候选 **4**，其中 **今天可联系 0**

### 2.3 两个必须先说清的硬事实

**① 「待激活」这条线目前是空转的。**
4 个历史候选全部卡在同一条规则：`history_unknown = True → 待核实`。原因是聊天详情页看不到历史联系次数，而系统（正确地）拒绝把"未知"当成"零"。结果就是日报里"今天可联系"永远是 0——**不是没有客户，是缺一个能证明联系历史的字段**。

**② 抓取通道还是最贵的那条。**
前面几轮日报都是 browser 子代理跑的。v1.1 文档自己写了"token 未计量"，v1.2 也写了"不承诺低消耗"。这跟你要的"消耗比较低"是冲突的，必须换掉。

---

## 三、低消耗的本质：把管道拆成四层

核心原则：**确定性的活交给程序，LLM 只干一次"读懂人话"**。

| 层 | 干什么 | 谁执行 | LLM 消耗 |
| --- | --- | --- | --- |
| **L1 列表层** | 抓新增询盘 + 待激活候选的列表与负责人 | 脚本化浏览器（状态机） | **0** |
| **L2 判定层** | 本地比对：新线索／买家新回复／承诺逾期／沉默到期 → 五态分类 | Python 确定性规则 | **0** |
| **L3 语义层** | 只对**进入候选**的客户读懂聊天，写"聊什么、做什么动作、完成标准" | 模型，**每轮 1 次** | **1 次** |
| **L4 渲染分发层** | 按业务员出动作卡并投递 | 模板渲染 + 渠道 | **0** |

关键：L3 是**唯一**花钱的地方，而且它对"今天不需要动作的 30 多个客户"完全不执行。这就是低消耗的来源——不是把模型用得更省，而是**让它少看**。

### 通道成本对比（为什么现在是贵的）

| 通道 | 抓取阶段 LLM | 说明 |
| --- | --- | --- |
| browser 子代理（**现用**） | 每一步都要模型指挥 → **高** | 翻页、点详情、读 DOM 都由模型驱动 |
| 询盘统计 skill 的 manager | **0** | 脚本化 Playwright + 状态机 |
| 谈单助手 CLI | **0**（抓取）＋ 1 次（分析） | Rust 二进制抓完，只把 1 份 analysis bundle 交给模型 |

---

## 四、抓取通道选型（**第一个要你拍板的**）

三条可用通道，粒度不同，不冲突：

| 通道 | 粒度 | 能拿到什么 | 适合 |
| --- | --- | --- | --- |
| **A** 询盘统计-0622外宣版 | 列表级 | 按时间窗／负责人／分类／买家名抓询盘列表与聊天，产出 `inquiries.json`；自带 cron 入口 `run_scheduled.py` | **每日全量新增** |
| **B** 谈单助手0826 | 单客户 | 询盘＋TM＋完整聊天＋OneTalk＋OKKI＋订单＋行为＋采购，6 份产物 | **少量深度分析** |
| **C** browser 子代理 | 任意 | 什么都能干，模型驱动 | 兜底 |

**我的建议：A 打底 + B 补深 + C 兜底。**

**为什么 B 很关键**：它的 `profile_json` 里含**订单／行为／采购／OneTalk／OKKI**——这些正是 §2.3① 缺的"历史联系证据"。如果 B 能补出联系次数，那个空转问题就能解开。本文件夹里 `captures/` 和 `deal-assistant-13942428901-report.html` 就是 B 在 2026-08-31 的实测产物，链路是通的。

**为什么需要你确认**：
- **A 依赖 Chrome 处于 `localhost:9222` 且有登录态。** v1 文档记录过 CDP 在"默认目录 + 既有 Profile"下绑不上 9222 的问题；记忆里后来用隔离目录 `chrome-alibaba-auto` 解决过。**这条要你确认现状**——能不能稳定连上，直接决定 A 能不能当底座。
- **B 按客户计费／耗时**，只适合少量，不能当每日全量通道。

---

## 五、「待激活」口径（**第二个要你拍板的**）

现行参数（`wake_rules.py` 的 `WAKE_CONFIG`，属保守初值，非行业定律）：

| 规则 | 当前值 |
| --- | --- |
| 默认沉默窗口 | 5 团队工作日 |
| 每周期唤醒上限 | 3 次 |
| 第 2 次距第 1 次 | ≥5 团队工作日 |
| 第 3 次距第 2 次 | ≥7 团队工作日 |
| 第 3 次后冷却 | ≥30 自然日 |
| 同一买家 90 天滚动 | ≤6 次 |

**缺口**：v1 文档写着"**待激活 6 条件**"，但本文件夹里**没有这 6 条的条目内容**（它来自当时的《讨论稿》附件，未落盘）。当前只实现了 3 项判据：**沉默到期 + 有需求证据 + 有可提供的方案**。

**请你补这 6 条**，或确认就沿用现在这 3 项。

另：工作日按 **周一～周五** 计算，**未接入法定节假日**。

---

## 六、分发方案（**第三个要你拍板的**）

当前可用渠道（系统实测）：

- **微信**：我的账号 ready
- **飞书**：已授权 1 人 ＋ 1 群；我自己的账号未配置

**卡点**：4 名业务员（Ella Chen／Alice Lam／Bin Cai／Alan Au）**都不在已授权名单里**，现在无法直接私聊发给他们。

两条路：

- **路线 1（推荐，先做）**：每天只发给你（管理者）一份合并日报 → 你过目 → 再决定是否二级分发。风险最低，且正好符合你"我来审核一遍"的习惯。
- **路线 2**：把 4 人加进授权名单，按人拆卡分别投递。需要你先在「消息渠道 > 授权管理」里加人。

无论哪条，都要你确认三件事：**渠道（微信／飞书）、接收人、发送时间**。

---

## 七、建议的落地分期

### Phase 1 —— 把抓取换成低消耗通道（先解决"贵"）
1. 接通通道 A（询盘统计 manager），跑通一次「今日新增」真实抓取。
2. 把 A 的输出接进 `daily_run.run_daily()` 的 batch 结构（接口已定义好，不用改架构）。
3. 定时间（建议每日 08:30 前跑完，业务员上班即见）。
4. **验收**：同一份数据，token 消耗显著低于 browser 子代理，且线索数不少于人工核对值。

### Phase 2 —— 让「待激活」真正出货（先解决"空转"）
1. 用通道 B 对 4 个历史候选各做一次深度抓取，确认能否取到历史联系次数。
2. 能取到 → 写入 `wakes.json`，`history_unknown` 解除，候选进入「今天可联系」。
3. 取不到 → 明确降级：待激活改为**人工标注历史次数**，或改用其他字段定义"可联系"。
4. **验收**：「今天可联系」不再是 0，且每一条都有可追溯证据。

### Phase 3 —— 分发（先解决"发不到人"）
1. 按业务员拆卡（`generate_report.py` 已按 `assignee_roles.json` 路由，只需加"单人版"渲染）。
2. 走路线 1 或路线 2 投递。
3. **验收**：业务员收到的卡里，"联系谁／聊什么／做什么／完成标准"四项齐全。

---

## 八、验收标准建议

| 维度 | 怎么算过 |
| --- | --- |
| 幂等 | 同一天跑两次，线索数／任务数／ID 集合不变（现有 40 项测试已覆盖） |
| 幂等（新增） | 真实抓取连续跑两天，不产生重复任务、不重复计数 |
| 不漏 | 新增询盘的召回率：与后台人工核对，差异必须逐条说明原因 |
| 不误伤 | 「不该提醒」的客户（约定暂停、已成交、已拒绝）不得出现在今日行动卡 |
| 低消耗 | 每轮 LLM 调用次数 ≤ 1（L3 之外为 0），且抓取阶段为 0 |
| 可追溯 | 每条动作卡都能点回原文证据（现有格式已满足） |

---

## 九、要你拍板的 6 件事

1. **抓取通道**：接受「A 打底 + B 补深 + C 兜底」吗？A 依赖的 `localhost:9222` 登录态现在是否稳定？
2. **待激活 6 条件**：能补上条目内容吗？还是先用现在的 3 项判据跑？
3. **沉默窗口**：还是 5 个团队工作日？要不要按客户等级分级（比如 L3/L4 短一点）？
4. **分发对象与渠道**：先只发你（路线 1），还是直接按人发（路线 2）？渠道用微信还是飞书？几点发？
5. **每日动作卡上限**：一名业务员一天最多推几条？（建议 3–5，防止刷屏导致免疫）
6. **深度分析预算**：B 通道（谈单助手）每天最多跑几个客户？（建议 0–2，只在需要判断历史联系或高价值分歧时触发）

---

## 十、本文的边界

- 本文**未执行任何抓取**，未发送任何消息，未修改任何代码或数据。
- 第 2 节所有数字来自本次实跑测试与 `state/` 文件实际读取，非文档转述。
- 第 2.3 节两条"硬事实"是本次盘点新发现的问题，此前的交付文档没有明确写出来。
- 覆盖缺口仍在：此前抓取只抽了 3 页（第 1、95、96 页），后台显示约 1912 条，**未全量扫描**。
