# 每日询盘跟进 v1.1 —— 交付与验证结果（最终版）

> 更新时间：2026-09-12
> 按评审《每日询盘跟进-v1试跑评审与下一轮要求》执行。暂未启用正式推送。

---

## 一、修正日报（已交付）

- 文件：[每日询盘跟进日报-修正-2026-09-11.md](每日询盘跟进日报-修正-2026-09-11.md)
- 生成方式：`python generate_report.py` 从 `state/tasks.json` **同一任务数据**自动汇总，日报与主管汇总同源。

### 7 处判断修正（对照评审第 2 节）

| 项目 | 原错误 | 修正后 |
| --- | --- | --- |
| Jens | 列入"今日逾期" | 承诺"两天内"→ due_at=2026-09-13，**未到期**，不称违约 |
| Paola | 标"紧急/逾期" | 无客户期限→ due_at=未知，按团队响应目标，不编造 |
| YANIS | 直接催"补发含运费报价" | **前置条件未满足**：缺地址/配置，等客户或精准补问 |
| Lina | 标"已闭环、无动作" | **订单收尾待办**：唛头未确认，保留确认+核对出货节点 |
| josh | 发 PDF 后"只建议等待" | 核验资料对应型号与问题，再安排确认（发文件≠解决） |
| Jonathan | 完成标准写"确认下单" | 完成标准=**规格（线长）确认**，下单是后续目标 |
| Sultana | "确认能否成交"空泛 | 解释运费构成/核验替代/确认接受条件（有聊天依据） |

每条任务保存：证据（原文/译文/判断三态分离）、触发原因、阻塞与责任方、前置条件、due_at、逾期状态、下一步、完成标准、下次检查时间。无期限=未知，不默认逾期。

### 统一计数

- 原始线索：20｜独立会话：17｜独立客户（按名，估计）：17｜商机：17｜待办：9
- 待办分布：承诺待兑现 2、高意向待推进 2、重复线索核实 2、前置条件未满足 1、订单收尾待办 1、待核查 1
- NSABIMANA ×3、Hashim ×2 标"重复线索核实"，不自动合并、不修改后台。

---

## 二、状态持久化（已实现）

- 位置：`F:/Accio agent date/谈单助手/state/`
  - `opportunities.json`（商机/线索）、`tasks.json`（任务）、`wakes.json`（唤醒/联系）、`checkpoints.json`（覆盖基线）、`assignee_roles.json`（负责人角色）
- 引擎：[state_engine.py](state_engine.py)；种子：[seed_state.py](seed_state.py)、[seed_silent.py](seed_silent.py)；报告：[generate_report.py](generate_report.py)
- 机制：稳定 task_id 幂等、人工状态保留、due_at 跨日计算、回复暂停唤醒、联系事件去重、section 原子写（部分失败不清空）。

---

## 三、跨日与重复运行验证（模拟测试，12/12 PASS）

[test_engine.py](test_engine.py)：重复运行任务/计数不变、ID 集合不变、跨日到期正确、联系去重、回复暂停唤醒、人工状态保留、部分失败不清空。实测 `seed_state.py`/`seed_silent.py` 二次运行 `new=0`。

> 模拟测试与真实抓取分开标注；真实数据受登录态与抓取成功率影响。

---

## 四、历史沉默客户识别（已完成）

- 走用户已登录 Ben 会话（浏览器子代理）只读扫描，共 **1912 条线索**，最旧更新时间 2025-09-06～09-08（约一年前）。
- 抽样第 96/95 页 20 条 + 4 条详情判定：
  - **唤醒候选（A）：3 条** — UmmarFaruk Patel（Alice Lam）、Fonda Arnaud Nanfack（Alice Lam）、Marina BN（Bin Cai）。共同点：买家有未满足需求后消失、最后为卖家（含模板跟进）发言、无约定暂停。
  - **未知（C）：1 条** — Miguel Arnaldo Molina Riveros（Alan Au），会话仅"Hi+转交通知"，无买家有效发言。
  - **约定暂停（B，不该唤醒）：0 条** — 本轮 4 条样本未发现"later/next month/暂停"约定。
- **历史联系次数**：详情页看不到，且可见"卖家模板跟进"证据 → 一律 `history_unknown=true`，不按零。因此这些是**唤醒候选**，非"确定该唤醒"。
- 缺口：`约定暂停（不该唤醒）`真实样本未找到，需用户提供已知暂停客户或扩大详情抽样。

---

## 五、实际消耗（如实）

- `询盘统计` skill 脚本：**0**（未运行）。
- 浏览器子代理：本轮共 4 次只读任务（列表、详情、沉默扫描-失败、沉默扫描-成功），**token 未计量**。
- 确定性规则/状态引擎/报告渲染：**零 LLM**。
- 未计量项不编造；不以未运行脚本的 0 消耗代替总成本。

---

## 六、覆盖缺口

1. 列表仅扫第 1 页（今日）+ 第 95/96 页（最旧沉默）；96 页全量未扫。分层：列表基线 + 仅对新增/变化/到期/待核查会话读详情。
2. 买家等级 L0–L4 列表页不可读（非 v1 阻塞，按采购证据/期限/未闭环排序，等级标未知）。
3. 内部任务阻塞（等样品/工厂交期/财务）聊天不可见，需 OKKI/CRM 或人工标注。
4. 同名≠合并，buyer_id 未取，客户去重为"按名估计"。
5. `约定暂停（不该唤醒）`样本缺失。

---

## 七、负责人映射（已解决）

- **销售（4 人）**：Ella Chen / Alice Lam / Bin Cai / Alan Au
- **运营（2 人，日报过滤）**：Ting Zen / yanjun Lin
- 已写入 `state/assignee_roles.json`。注：实际为 **4 名销售**（非评审所写"3 名"）。

---

## 八、剩余待办

1. 补充 `约定暂停（不该唤醒）`真实样本，验证"不该提醒"逻辑（你提供已知暂停客户，或我扩大详情抽样）。
2. 可选：专用目录 `chrome-alibaba-auto` 登录一次，供未来 skill 脚本直连 9222（当前浏览器子代理已够用）。
