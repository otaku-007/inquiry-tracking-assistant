# v1.3 Accio Work 询盘助手执行规格

> **文档性质**：正式产品与技术执行规格（待老板审核）
> **基线**：v1.2.1（git tag `v1.2.1`，commit `0cb32ab`，61 项测试全绿，已冻结）
> **本版范围**：只定义 v1.3 要做什么、怎么做、做到什么程度算完成。**不含任何代码变更。**
> **编写依据**：全仓库代码（state_engine / wake_rules / daily_run / generate_report / seeds / test_engine，共 61 项测试）、state/ 真实数据（40 线索 / 12 任务 / 4 唤醒候选）、captures/ 谈单助手抓取产物、v1.1–v1.2.1 全部交付文档
> **创建日期**：2026-09-17
> **修订记录**：2026-09-17 老板审订三处修正——① Wake 执行语义四态化（`due / reported / sent / cancelled`，§3.1–3.2，取消"报告出现行动卡=已执行"）；② 等待阶段中卖家主动消息三分类周期边界（`followup_wake / substantive_update / courtesy`，§3.3、§7.2）；③ checkpoint 增加 `run_type` / `run_status`（仅 `success` 视为当日完成，§5.3）。除这三处修正外未扩大需求。

---

## 0. 文档定位与现有资产

### 0.1 需求覆盖对照

本文按工程结构重组了需求原文，逐条映射如下，便于审核时核对无遗漏：

| 需求原文章节 | 本文章节 |
| --- | --- |
| 一、产品最终运行方式 | §1 |
| 二、系统主要需要识别的客户状态 | §2 |
| 三、重新设计唤醒周期模型 | §3.1–3.2 |
| 四、AI 必须识别"唤醒周期边界" | §3.3–3.5 |
| 五、浏览器数据层 | §4 |
| 六、最近 30 天增量扫描 | §5 |
| 七、长期商机记忆 | §6 |
| 八、AI Analyzer | §7 |
| 九、禁止 AI 编造业务事实 | §7.4（约束 A1–A7） |
| 十、不开发报价系统 | §1.4、§7.3 |
| 十一、报告结构 | §8 |
| 十二、Shadow Mode | §9 |
| 十三、工程要求 | §10 |
| 十四、验收标准 | §12 |

### 0.2 v1.2 / v1.2.1 资产盘点

| 模块 | v1.3 中的去向 | 说明 |
| --- | --- | --- |
| `state_engine.py` 幂等合并 / 损坏保护 / 原子写 / 备份 | **保留复用** | upsert、stable_id、备份、损坏保护模式直接沿用 |
| `state_engine.py` 任务到期刷新（refresh_overdue 等） | **保留复用** | 跨日到期逻辑不变 |
| `wake_rules.py` decide_wake 及 WAKE_CONFIG | **重写** | 3 次/5-7 工作日/30 冷却/90 天滚动 6 次 → 3/5/5/30 自然日显式周期（§3） |
| `wake_rules.py` buyer_key / aggregate_buyer_contacts | **保留思想，随数据模型重构** | v1.2.1 的教训（写入键=查询键）成为设计原则 |
| `wake_rules.py` record_wake_contact（含 prior_count） | **废弃，由周期实体取代** | v1.2.1 修复的原则被继承：**计数不得从永久 contacts 历史派生**，v1.3 直接显式记录每次唤醒事件到周期实体 |
| `generate_report.py` 渲染骨架 | **保留骨架，重写内容** | 单一日报 → 老板报告 + 业务员报告两个渲染器 |
| `daily_run.py` run_daily 流程（备份→读取→合并→保存→渲染） | **保留骨架，接捕获器** | batch 手工构造 → 由捕获器 + AI Analyzer 自动产出 |
| `seed_state.py` / `seed_silent.py` | **降级为测试夹具** | 不再是生产数据来源；保留用于回归测试 |
| `test_engine.py` 61 项测试 | **保留全部通过** | 新模块新增各自测试；旧测试随重写模块迁移 |
| `state/assignee_roles.json` 角色路由 | **保留复用** | sales: Alice Lam / Alan Au / Bin Cai / Ella Chen；operations: Ting Zen / yanjun Lin |
| `state/` 现有 40 线索 / 12 任务 / 4 唤醒候选 | **迁移保留** | 迁移规则见 §6.3；4 个 history_unknown 候选保持待核实（D6） |
| `captures/` 谈单助手产物 | **参考样本** | 其中 `login_id` 是 buyer_id 调查的现成线索（§4.2） |

### 0.3 术语表

| 术语 | 定义 |
| --- | --- |
| 有效消息（effective message） | 需要对方响应的实质内容。卖家侧：报价、答复、资料、方案、明确提问；**不含**自动接待/机器人消息、系统通知、纯礼貌收尾 |
| 实质回复（substantive reply） | 买家回复中真正改变商机状态的内容（§3.4）；OK/Thanks/👍/noted/seen 不必然算 |
| 等待阶段（waiting phase） | 从我方最后一条有效消息起、到我方收到实质回复或发出新有效消息为止的阶段 |
| 唤醒周期（wake cycle） | 一个等待阶段内的 4 次唤醒序列（3/5/5/30 规则）及其状态机 |
| 30 天窗口 | 浏览器**扫描**窗口（最近 30 天有联系的会话）；**不是**记忆保留期限（§1.3） |
| Shadow Mode | 每天真实抓取与分析，但报告只给管理员、不外发、不联系客户（§9） |
| checkpoint | 每次运行的快照记录（扫描范围、diff 结果、报告基线），用于增量比对与补跑幂等 |
| 唤醒事件状态 | `pending / due / reported / sent / cancelled`（§3.2）。`reported`（已入业务员报告）≠ 已执行；仅 `sent`（聊天流实际检测到跟进消息）推进周期 |
| 卖家消息三分类 | 等待阶段中卖家主动消息分为 `followup_wake / substantive_update / courtesy`（§3.3），决定其周期边界效果 |
| run_type / run_status | checkpoint 的运行类型（scheduled/catchup/manual）与运行结果（running/success/partial/failed）；仅 `success` 视为当日完成（§5.3） |

---

## 1. 产品运行方式（Accio Work）

### 1.1 运行环境与触发

| 项 | 规格 |
| --- | --- |
| 宿主 | Accio Work Skill |
| 触发 | 每天 **09:30**（Asia/Shanghai）自动执行 |
| 补跑 | 09:30 时电脑未开机 → 开机并启动 Accio Work 后，检查当日 checkpoint：**当日尚无 `run_status == success` 的记录则立即补跑**；补跑结果为 `partial` / `failed` 时，修复后允许再次补跑（幂等由 checkpoint 与稳定 ID 保证，不产生重复实体，§5.3） |
| 登录 | Alibaba 国际站**公司主账号**（可见全部子账号/业务员会话），浏览器会话沿用隔离目录模式（沿用既有 `chrome-alibaba-auto` 经验，防自动升级破坏登录态） |
| 扫描范围 | 全店会话（所有 owner）；owner 字段决定报告归属，owner 变更视为 CHANGED |
| 时区 | 所有日期/时间统一北京时间存储与展示；唤醒规则的"天"为**自然日**（v1.2 的工作日/节假日历概念随之废弃） |

### 1.2 每日任务流水线

```
09:30 定时/开机补跑
   │
   ├─ 1. 打开国际站后台 →「商机沟通」
   ├─ 2. 轻量扫描：最近 30 天有联系记录的会话列表（§5.1 第一阶段）
   ├─ 3. 与上次 checkpoint 比对 → NEW / CHANGED / UNCHANGED
   ├─ 4. 合并重入集：今日任务到期 / 唤醒到期 / 约定联系日到期 / 待人工复核（§5.3，可超 30 天）
   ├─ 5. 仅对候选会话打开聊天详情，抓取完整上下文（§4 数据契约）
   ├─ 6. AI Analyzer 逐客户分析（§7）：状态判定、实质回复判定、周期边界判定
   ├─ 7. 确定性引擎更新长期记忆（§6）：周期推进、任务创建/关闭、幂等去重
   ├─ 8. 判定今天各业务员应处理哪些客户
   ├─ 9. 生成老板总报告（§8.1）
   ├─ 10. 按 owner 生成业务员行动报告（§8.2）
   ├─ 11. 写 checkpoint（run_type / run_status / 扫描范围 / diff / 报告基线）
   └─ 12. Shadow Mode：报告仅交管理员；稳定后接入飞书/微信分发（§9）
```

硬性边界（第 12–13 条需求）：
- **不自动给客户发送任何消息**。推荐回复只生成在业务员报告里，仅供参考，由业务员复制后人工发送。
- 系统对国际站后台的写入操作为**零**（不发消息、不改状态、不合并线索）。

### 1.3 30 天窗口与长期记忆的关系（重入规则）

**30 天只是浏览器主动扫描窗口，不是商机记忆保存期限。**

每日处理集 = 30 天窗口内的 NEW/CHANGED 会话 ∪ 重入集。重入集包含（即使该客户超 30 天无任何新聊天）：

1. 今天有到期复查任务（`next_check_at` / `due_at` ≤ 今天）；
2. 今天到达唤醒时间（wake 1/2/3/final 任一到期）；
3. 客户约定的联系日期（`pause_until`）今天到期；
4. 状态被标记为待人工复核。

重入会话通过保存的 `conversation_url` 直接进入详情抓取（§5 步骤 4→5），不需要出现在 30 天列表扫描结果中。

### 1.4 系统边界（永远不做什么）

1. 不自动发送客户消息（含唤醒消息——唤醒是**提醒业务员去联系**，不是系统代发）；
2. 不开发/连接报价系统，只识别报价状态（§7.3）；
3. AI 不编造业务事实（§7.4）；
4. 不做大型 UI、复杂 CRM 功能；
5. 不自动合并/修改国际站后台线索。

---

## 2. 客户状态模型

### 2.1 十一种状态定义

每个会话（conversation）在每次分析后必须落在一个且仅一个**主状态**。状态由 AI Analyzer 判定（输出 `customer_state`），确定性代码校验与流转。

| # | 状态 | 判定依据 | 等待方 |
| --- | --- | --- | --- |
| S1 | 业务员未回复 | 最后一条有效消息来自买家，业务员尚无有效回复（**机器人/自动接待消息不算业务员回复**） | 卖家 |
| S2 | 待报价 | 买家已明确提出报价需求，业务员尚未报价也未承诺 | 卖家 |
| S3 | 报价准备中 | 业务员已承诺报价，等待内部价格/产品资料/运费等 | 内部 |
| S4 | 已报价待回复 | 报价已发出，等待买家回应 | 买家 |
| S5 | 需唤醒 | 普通沟通后客户持续无实质回复，处于唤醒周期中（§3） | 买家 |
| S6 | 约定再联系 | 客户明确约定未来某日再谈（`pause_until`） | 时间 |
| S7 | 等待客户提供资料 | 等买家给地址/规格/图纸/参数等 | 买家 |
| S8 | 等待内部处理 | 等内部核价/打样/产线确认/认证核验等 | 内部 |
| S9 | 拒绝/停止联系 | 客户明确拒绝或要求停止（`stop_contact`） | — |
| S10 | 待人工核实 | AI 无法可靠判断（`requires_human_confirmation` 或置信度不足） | 人工 |
| S11 | 唤醒周期已结束 | 4 次唤醒均实际发送（`actual_sent_at` 确认）后仍无实质回复，周期 ended，不再自动产生唤醒任务 | 买家主动 |

### 2.2 与 v1.2 action_type 的映射（迁移期参考）

| v1.2 action_type | v1.3 状态 |
| --- | --- |
| 待回复 | S1 |
| 承诺待兑现 | S3 / S8（按等待对象拆分） |
| 高意向待推进 | S2 / S4 / S7 / S8（按实际阻塞点归位） |
| 待核查 | S10 |
| 重复线索核实 | S10（保留任务类型，不映射为主状态） |
| 前置条件未满足 | S7 |
| 订单收尾待办 | S8（收尾类） |
| 本次会话已闭环 | 任务终态；主状态由 Analyzer 重判 |
| （v1.2 无） | S2 / S4 / S5 / S6 / S9 / S11 为新增状态 |

要点：v1.2 的 action_type 是**任务类型**，v1.3 的 customer_state 是**会话状态**——两者分离。任务从状态 + 阻塞点派生，任务类型体系保留并扩展。

### 2.3 已读未读的定位

- `read_status` / `unread_count` **必须抓取并保存**（§4.1），用于判断与报告展示（例如"报价已发 5 天、买家已读未回"）；
- **暂不作为进入唤醒流程的必要条件**——唤醒判定只依赖实质回复，不依赖已读；
- 抓取可行性本身是阶段 1 调查项（D7）：若平台不提供买家已读回执，明确记录"不可行"并从契约移除，不硬造。

---

## 3. 唤醒周期模型（v1.3 正式规则）

### 3.1 时间规则

全部为**自然日**。设卖家最后一条有效消息（周期锚点）为 T，第 1 次唤醒**实际发送日**（`actual_sent_at` 所在日）为 W1：

```
T（卖家最后有效消息）
 │  客户连续 3 天无实质回复
 ├─ 3 天 → WAKE 1 到期（T+3）→ 报告提醒（reported）→ 实际发送（actual_sent_at）
 │         实际发送后仍无实质回复
 ├─ 5 天 → WAKE 2 到期（WAKE 1 actual_sent_at + 5）→ 实际发送
 │         实际发送后仍无实质回复
 ├─ 5 天 → WAKE 3 到期（WAKE 2 actual_sent_at + 5）→ 实际发送
 │         实际发送后仍无实质回复
 ├─ 从 WAKE 1 actual_sent_at 起算第 30 天 → FINAL WAKE 到期（第 4 次，最后一次）→ 实际发送
 │         实际发送确认且之后仍无实质回复
 └─ CYCLE ENDED（周期结束，不再自动产生新唤醒任务）
```

规则细节：

| 项 | 规则 |
| --- | --- |
| WAKE 1 到期日 | T + 3 天（自然日） |
| WAKE 2 到期日 | WAKE 1 `actual_sent_at` + 5 天（依**实际发送时间**计算，不依报告生成时间） |
| WAKE 3 到期日 | WAKE 2 `actual_sent_at` + 5 天 |
| FINAL WAKE 到期日 | WAKE 1 `actual_sent_at` + 30 天（与中间轮次相互独立；D4） |
| 周期结束 | FINAL WAKE **实际发送确认**（`actual_sent_at` 落定）后即标记 ended（end_reason=`no_substantive_reply_after_final_wake`；D4） |
| 提醒 ≠ 执行 | 报告出现唤醒行动卡（`reported`）不代表已执行；仅聊天流检测到对应有效跟进消息（`sent`）才推进周期与下一轮 due_at（§3.2 约束 3） |
| 中断 | 任意时点买家实质回复 → 周期**立即**结束（end_reason=`substantive_reply`，未发送轮次置 `cancelled`），重新分析（§3.4） |
| 补跑 | 某轮到期日未运行（电脑关机）→ 次日运行时立即补提示该轮；后续轮次从该轮 `actual_sent_at` 起算 |
| 长期停机退化 | 若停机导致中间轮次逾期严重（如 WAKE 2/3 到期日已晚于 FINAL WAKE 日），当日**只保留最紧急一轮**（直接推进 FINAL WAKE），不连续提示多条 |

### 3.2 周期实体 schema

每个会话同一时刻至多一个**活跃**周期；历史周期永久保留。

```json
{
  "cycle_id": "wc_<stable_id>",
  "conversation_id": "<平台会话ID>",
  "buyer_id": "<稳定买家标识>",
  "cycle_status": "waiting_wake1 | wake1_sent | wake2_sent | wake3_sent | final_sent | ended",
  "trigger_message_id": "<锚点：卖家最后一条有效消息ID>",
  "trigger_message_at": "2026-09-10T17:42:00+08:00",
  "cycle_started_at": "2026-09-10",
  "wake_events": [
    {"seq": 1, "wake_kind": "wake1", "due_at": "2026-09-13",
     "status": "sent", "reported_at": "2026-09-13T09:35:00+08:00",
     "actual_sent_at": "2026-09-13T14:20:00+08:00",
     "source_message_id": "msg_8831", "task_id": "..."},
    {"seq": 2, "wake_kind": "wake2", "due_at": "2026-09-18",
     "status": "pending", "reported_at": null, "actual_sent_at": null,
     "source_message_id": null},
    {"seq": 3, "wake_kind": "wake3", "due_at": null, "status": "pending"},
    {"seq": 4, "wake_kind": "final", "due_at": null, "status": "pending"}
  ],
  "first_wake_sent_at": "2026-09-13",
  "second_wake_sent_at": null,
  "third_wake_sent_at": null,
  "final_wake_sent_at": null,
  "last_substantive_reply_at": null,
  "ended_at": null,
  "end_reason": null
}
```

**WakeEvent 字段语义**（四态生命周期）：

| 字段 | 说明 |
| --- | --- |
| `due_at` | 该轮到期日。WAKE 1 = T+3；WAKE 2/3 = 上一轮 `actual_sent_at` + 5 天；FINAL = WAKE 1 `actual_sent_at` + 30 天。**只由确定性代码写入** |
| `reported_at` | 该轮唤醒行动卡首次出现在业务员报告的时间。**不代表已执行** |
| `actual_sent_at` | 后续在国际站聊天流中实际检测到业务员发送的对应有效跟进消息的时间（`source_message_id` 指向该消息）。**这才是执行**，是推进周期的唯一依据 |
| `source_message_id` | 实际发送的那条跟进消息的 message_id；`status != sent` 时为 null |
| `status` | 事件状态机：`pending`（未到期）→ `due`（已到 due_at）→ `reported`（已入报告，尚未发送）→ `sent`（实际发送确认）→ 终态；`cancelled`（因买家实质回复、买家明确拒绝/停止联系、周期被 `substantive_update` 重开等原因取消，可从任意非 sent 态进入） |

状态流转约束：

1. `due → reported → sent` 单向推进；`sent` 为该轮终态，不可回退；
2. **仅 `sent` 推进 `cycle_status` 与下一轮 `due_at` 计算**——下一轮 due_at = 本轮 `actual_sent_at` + 对应天数，**绝不依 reported_at / 报告生成时间计算**；
3. FINAL WAKE 与中间轮同理：**以实际跟进消息确认发送（`sent` + `actual_sent_at` 落定）为周期自动唤醒完成的判定**；仅 reported 的 FINAL WAKE 不得将周期置为 ended；
4. 业务员**未按报告行动**（长期停在 reported）不影响状态机正确性——该轮持续在每日报告重提，周期不推进，直至实际发送或取消条件出现。

设计约束（继承 v1.2.1 教训，并落实老板审订）：

1. **当前轮次只从 `wake_events[].status` 与 `cycle_status` 读取，绝不从 contacts 数量推算**；
2. `due_at` 只由确定性代码在唤醒事件**实际发送后**（`actual_sent_at` 落定）写入下一轮（AI 无权写，§7.4）；
3. ~~唤醒"执行"的定义 = 业务员报告里出现该唤醒行动卡~~（**已取消**）。系统不自动给客户发送消息，"执行"的唯一确认是**后续在国际站聊天中实际检测到业务员发送的对应有效跟进消息**（`status=sent` + `actual_sent_at` + `source_message_id`）；报告出现行动卡仅代表 `reported`。

### 3.3 周期开始与边界（AI 判定 + 状态机执行）

**周期开始**：业务员发出一条需要客户响应的有效消息，且此后客户无实质回复。等待阶段由 AI 判定开始（输出 `waiting_phase_started` + `trigger_message_id`），状态机创建周期。

**连续多条卖家消息不分裂周期**。示例：

```
10:00 Seller: Here is our quotation.
10:02 Seller: Lead time is 20 days.
10:03 Seller: Please check.
```

三条消息属于**同一个等待阶段**。处理规则：

- 等待阶段内，业务员的**后续补充消息仍需三分类**（本节下方规则）；`substantive_update` 移动锚点（T 更新为该消息时间）并结束旧等待锚点、重开 3 天计算；
- 纯礼貌性/无实质内容的卖家消息（`courtesy`，如 "Thanks" "OK"）既不移动锚点也不计为唤醒轮次；
- 等待阶段尚未开始（周期未创建）时，卖家的连续消息按 §3.3 周期开始规则合并为同一等待阶段（锚点=最后一条有效消息）。

**等待阶段中卖家主动新消息的三分类**（替代旧的"一律占轮次"单一规则；本阶段只定义数据结构与规格，分类判定由后续 AI Analyzer 实现，§7.2）：

| 类别 | 定义 | 周期效果 |
| --- | --- | --- |
| `followup_wake` | 为执行当前唤醒轮次而发出的跟进消息（对应报告里的唤醒行动卡） | **属于当前 wake cycle**；占用当前应执行轮次；该轮 `status=sent`、`actual_sent_at` = 真实消息时间、`source_message_id` = 该消息 ID；下一轮 due_at 从该 `actual_sent_at` 起算 |
| `substantive_update` | 提供新的价格条件、交期、产品方案或其他重要业务信息的有效消息 | **结束旧 waiting phase**（未发送轮次置 `cancelled`，周期 end_reason=`seller_substantive_update`）；以新有效消息重建等待锚点（新 trigger_message_id）；**后续重新从 3 天起算**（即开新周期或按周期创建规则重建） |
| `courtesy` | 无实质内容的礼貌性消息（"Thanks" "OK" 等） | 不移动周期锚点、不占 wake 轮次、不影响 wake_events |

分类边界：

- 判定入参：消息文本 + 上下文（该轮唤醒是否 due/reported、是否呼应买家此前的询点）；
- `followup_wake` 的认定以**对应关系**为准：消息可关联到当前 due/reported 的唤醒轮次（语义上是"无新信息、主动跟进"）；若无法关联且具备新业务信息 → `substantive_update`；
- 分类结果存入 Decision（可追溯，§6.2）；`seller_message_class` ∈ {`followup_wake`, `substantive_update`, `courtesy`} 是 Analyzer 输出契约字段（§7.2）；
- 数据结构先行：WakeEvent 的 `actual_sent_at` / `source_message_id` 字段即为此设计——无论消息由报告引导（业务员照行动卡发送）还是业务员自发跟进，**统一以聊天流中检测到的真实消息为准**回填。

**周期创建的幂等**：同一 `trigger_message_id` 只能创建一个周期；重复分析输出同一周期。

### 3.4 实质回复判定

**通常算实质回复**：回答产品问题、给出数量、给出目标价格、确认产品、询问价格、询问交期、提供地址、继续谈项目、明确拒绝、约定未来联系时间。

**不一定算**（需 AI 结合上下文判断是否真正改变商机状态）：OK / Thanks / 👍 / noted / seen。

判定要求：

- 逐条买家新消息输出 `substantive_reply: true/false` + `substantive_reply_reason` + 证据引用；
- "明确拒绝"也算实质回复——它结束唤醒周期并转入 S9（停止联系），而不是继续唤醒；
- 置信度不足时输出 `requires_human_confirmation`，宁可人工核实（S10）不可误判。

**实质回复发生后的处理**（确定性代码执行）：

1. 活跃周期立即结束（`end_reason=substantive_reply`，`last_substantive_reply_at` = 该回复时间）；
2. 重新分析：当前需求、商机阶段、业务员是否需要回复；
3. 若分析结论为"业务员需要回复"→ 状态 S1，**不产生新周期**（周期只针对"我方已回复、客户沉默"）；
4. 之后业务员发出新的有效消息且客户再度沉默 → 才形成新等待阶段、开新周期，计数从零开始。

### 3.5 周期结束条件汇总

| end_reason | 触发 | 后续 |
| --- | --- | --- |
| `substantive_reply` | 任意轮次中买家实质回复 | 重新分析，正常流程 |
| `seller_substantive_update` | 周期中途卖家发出 `substantive_update` 类消息 | 旧周期结束（未发送轮次 `cancelled`）；新等待锚点 + 重新从 3 天起算（§3.3 三分类） |
| `no_substantive_reply_after_final_wake` | FINAL WAKE **实际发送确认后**仍无实质回复 | S11，不再自动唤醒；买家日后主动来信则正常处理，新等待阶段可开新周期 |
| `stop_contact` | 买家明确拒绝 | S9，未发送轮次置 `cancelled` |

### 3.6 旧规则处置

v1.2 的以下规则在 v1.3 中**默认废弃**（新 3/5/5/30 已完整覆盖节奏）：

- 周期 3 次上限 / 5-7 **工作日**间隔 / 30 天冷却后"需新事实才可重开"；
- 90 天滚动 6 次客户级限次（D2：是否保留为观察指标，待定）；
- 沉默 5 工作日判定窗口、工作日/节假日历（全部改自然日）。

历史数据兼容：现有 4 个 wake 候选全部 `history_unknown=True` 且无消息级历史，**无法锚定周期** → 保持 S10 待核实，待其产生新消息后按新模型建周期（D6）。

---

## 4. 浏览器数据层（阶段 1 核心）

### 4.1 数据契约（v1.3 第一批字段）

| 字段 | 来源层 | 必需性 | 说明 |
| --- | --- | --- | --- |
| lead_id / inquiry_id | 列表 | 必需 | 询价单号（现有体系的主键） |
| buyer_id | 列表/详情 | **必需（本阶段调查目标 D1'）** | 平台稳定买家标识；禁止长期用 buyer_name 冒充 |
| conversation_id / chat_id | 详情 | 必需 | 会话主键；详情 URL 中的哈希段（如 `MC1IDX1y7uTjaQ-...`）是候选 |
| buyer_name | 列表 | 必需 | 展示用 |
| company | 详情 | 应有 | 公司名 |
| country | 列表/详情 | 应有 | |
| owner | 列表 | 必需 | 报告归属；owner 变更 → CHANGED |
| last_message_time | 列表 | 必需 | **语义待 D3 调查**（Marina 问题） |
| last_message_sender | 列表/详情 | 必需 | 判断 S1 的关键 |
| read_status / unread_count | 详情 | 应有（可行性=调查项 D7） | 保存用于报告；暂不作为唤醒必要条件 |
| list_update_time | 列表 | 必需 | 原样保存，**不推导沉默天数**（D3 确认前） |
| conversation_url | 详情 | 必需 | 重入抓取入口（§1.3） |
| product / inquiry source | 列表/详情 | 应有 | 产品名、来源渠道 |
| message_id / message 时间 / sender / 文本 | 详情 | 必需 | 消息级主键与内容，周期锚点依赖 |

### 4.2 buyer_id 调查路线（优先级排序）

**现有证据**：`captures/` 谈单助手产物中，TM 联系人对象带有 `login_id` 字段（样本值 `es39191246897ysts`）——这是平台侧稳定买家登录标识的**实证存在**。

调查顺序：

1. **列表页 DOM/XHR**：商机沟通列表的接口 JSON（Fetch/XHR 面板）中查找买家标识字段（`loginId` / `buyerId` / `memberId` / `contactId`）；
2. **详情页 URL 与 DOM**：`maDetail.htm?imInquiryId=...#feedback/detail/all/.../<哈希>/` 中的哈希段；详情页买家卡片/头像链接的 DOM 属性（`data-*`）与 profile URL；
3. **TM/OneTalk 消息管理器**：`messageManager.htm` 的联系人接口（谈单助手样本已证明 `login_id` 在此出现）；
4. **buyer profile 页**：`linkpage` / 买家主页 URL 参数；
5. **兜底**：以上全部不可得时，`conversation_id` 哈希作为会话级稳定键，`buyer_id` 置空并标注 `identity_unverified`（沿用 v1.2.1 的 `identity_verified=False` 语义，**不宣称客户级判定可信**）。

验收：同一买家跨多次扫描 `buyer_id` 稳定（§12.1）。

### 4.3 Marina 更新时间语义调查（D3）

已记录的矛盾：Buyer_O 列表 `update_time=2025-09-08`，详情最后消息 `2026-04-26 03:34`——同一字段在两条路径下语义不一致。

调查方法：

1. 在浏览器中制造受控变化（自己发一条测试消息给同事账号的会话 / 等待真实买家来信），观察列表 update_time 与各候选语义（最后聊天时间 / 询盘更新 / lead 更新 / 客户资料更新 / 系统状态更新）的联动；
2. 对比 XHR 原始字段名与页面展示值；
3. 结论写入本节附录：字段真实语义 + 可否用于推导沉默。

**冻结规则：语义确认前，任何代码不得用 `list_update_time` 推导"沉默 N 天"**（只能作为 CHANGED 检测的触发信号之一）。

### 4.4 抓取分层与消耗控制

| 层 | 动作 | 频率 | LLM 参与 |
| --- | --- | --- | --- |
| L0 登录态维护 | 隔离目录、登录检测 | 每次 | 零 |
| L1 列表扫描 | 商机沟通列表页 30 天窗口（分页） | 每日 | 零（纯 DOM/XHR 解析） |
| L2 详情抓取 | 仅候选会话（§5.3） | 每日 | 零 |
| L3 AI Analyzer | 仅候选会话 | 每日 | **每会话 1 次调用** |
| L4 报告渲染 | 本地模板 | 每日 | 零 |

消耗目标：LLM 只出现在 L3；L1/L2 全部脚本化。同一天 UNCHANGED 会话不进 L3。

---

## 5. 增量扫描（两阶段）

### 5.1 第一阶段：轻量扫描 + checkpoint 比对

1. 抓取列表（30 天窗口，含分页），产出原始行集；
2. 与上次 checkpoint 的列表快照比对，按会话维度分类：
   - **NEW**：本地无此会话；
   - **CHANGED**：last_message_time / last_message_sender / unread_count / owner 任一变化；
   - **UNCHANGED**：以上全部未变。
3. 比对键：`conversation_id`（兜底 `inquiry_id`）；
4. 触发信号以**详情页实抓数据为准**，列表字段只做初筛（防止 Marina 类字段语义问题造成漏判）。

### 5.2 第二阶段：深度分析候选集

进入详情抓取 + AI 分析的会话，仅为以下五类：

1. NEW；
2. CHANGED；
3. 今日本地任务到期（`next_check_at`/`due_at` ≤ 今天，含 30 天外重入）；
4. 今日唤醒轮次到期（wake_events 有 due_at ≤ 今天且 status ∈ {pending, due, reported}——**reported 但未 sent 的轮次持续重提**）；
5. 状态为待人工复核（S10）/ 约定联系日今天到期（S6）。

**UNCHANGED 且无本地到期任务的会话，不进详情、不进 AI 分析。**

### 5.3 checkpoint 结构

```json
{
  "run_id": "...",
  "ran_at": "2026-09-17T09:30:05+08:00",
  "run_type": "scheduled | catchup | manual",
  "run_status": "running | success | partial | failed",
  "as_of": "2026-09-17",
  "scan_window": {"days": 30, "pages": 6, "rows": 128},
  "list_snapshot": {
    "<conversation_id>": {
      "lead_id": "...", "buyer_id": "...", "owner": "...",
      "last_message_time": "...", "last_message_sender": "...",
      "unread_count": 0, "list_update_time": "..."
    }
  },
  "diff": {"new": [...], "changed": [...], "unchanged": [...], "reentry": [...]},
  "deep_analyzed": ["<conversation_id>", "..."],
  "report_baseline": {"boss_report_sha256": "...", "owner_reports": {"Ella Chen": "..."}},
  "errors": [...]
}
  ```

字段语义：

| 字段 | 取值 | 说明 |
| --- | --- | --- |
| `run_type` | `scheduled` / `catchup` / `manual` | 定时 09:30 触发 / 开机补跑 / 人工手动触发（调试、老板要求重跑等） |
| `run_status` | `running` / `success` / `partial` / `failed` | 运行中（启动时即写入，防同日并发重复启动）/ 全部完成 / 部分完成（如部分页扫描失败、部分会话分析失败但主流程完成）/ 失败（主流程未完成） |

补跑判定（老板审订后规则）：**当日已完成的唯一标准 = 当天存在 `run_status == success` 的 checkpoint**。当日只有 `running` / `partial` / `failed` 或无记录时，均允许（修复后）继续补跑；补跑产生新 run_id，幂等由 checkpoint 与稳定 ID 保证（不产生重复实体、不重复推进周期），含报告 SHA 供审计。

---

## 6. 长期商机记忆

### 6.1 实体关系

```
Customer (buyer_id 主键)
   └─ Opportunity (lead_id / inquiry_id)
        └─ Conversation (conversation_id)
             ├─ Messages (message_id)
             │     └─ ProductRequirement（从消息提取）
             ├─ Tasks (task_id)
             └─ WakeCycle (cycle_id)
                  └─ ContactEvents（每次唤醒执行/业务员实际联系）
                       └─ Decision / Evidence（AI 判定与依据，可追溯）
Run / Checkpoint（每次运行的扫描快照与报告基线）
```

### 6.2 实体职责与关键约束

| 实体 | 主键 | 关键字段 | 约束 |
| --- | --- | --- | --- |
| Customer | buyer_id（无则临时键+`identity_unverified`） | name, company, country, first_seen_at | 永不删除（30 天窗口外不清理） |
| Opportunity | lead_id | owner, category, source, create_time | owner 变更 → CHANGED + 报告重分组 |
| Conversation | conversation_id | url, last_message_time, last_message_sender, read_status | 重入入口 |
| Message | message_id | at, sender, kind(有效/礼貌/机器人/系统), text, substantive(仅买家) | 只追加，不改写 |
| ProductRequirement | 需求+会话 | products, quantity, requirements | 缺失→`unknown`，**禁止 AI 编造** |
| Task | task_id（stable_id 保持） | status, due_at, next_check_at, next_action, evidence | 幂等 upsert（沿用 v1.2 引擎） |
| WakeCycle | cycle_id | §3.2 schema | 同一时刻至多 1 个活跃周期 |
| ContactEvent | event_id（沿用 stable_id 去重） | at, kind, actor, task_id | 与报告生成解耦，记录业务员真实联系 |
| Decision | decision_id | analyzer 版本、判定、置信度、证据引用 | 每次 AI 分析一条，可追溯 |
| Run/Checkpoint | run_id | §5.3 | 补跑判定依据 |

### 6.3 现有 state/ 迁移

| 现有 | 去向 |
| --- | --- |
| opportunities.json（40 条） | → Customer（按 buyer 聚合，identity_unverified）+ Opportunity + Conversation；缺 buyer_id 留待补齐 |
| tasks.json（12 条） | → Task 实体（task_id 稳定，直接迁移；v1.2 action_type 保留为 task kind） |
| wakes.json（4 条 history_unknown） | → 不建周期；转 S10 待人工核实 + 保留原 note 为 Decision 证据 |
| checkpoints.json | → 新 checkpoint 结构；旧 runs 记录归档 |
| assignee_roles.json | 原样复用 |

迁移为一次性脚本 + 迁移后校验（实体计数守恒、外键完整），**旧 state/ 目录整体备份后只读保留**。

---

## 7. AI Analyzer

### 7.1 位置与职责边界

```
浏览器抓取（结构化 JSON） → 【AI Analyzer】 → 确定性引擎（状态机/日期/次数/幂等） → 记忆 → 报告
```

**AI 负责**：理解事实与上下文——状态判定、实质回复判定、周期边界判定（等待阶段是否形成）、需求提取、下一步建议、推荐回复草稿。

**确定性代码负责**：日期、次数、周期推进、任务状态、幂等、去重、到期判断。**AI 不允许直接修改联系次数或日期计数**——AI 的输出里只有判定与建议，落库的 due_at/actual_sent_at/status/cycle_status 由状态机写入。

### 7.2 输入输出契约

输入（每会话一次）：

- 客户资料 + 当前商机记忆（Customer/Opportunity/Conversation 摘要）；
- 新消息（自上次分析以来的全量，message_id 标识）；
- 最近必要历史上下文（最近 N 条 + 关键历史 Decision 摘要）；
- 已有任务（待办清单）；
- 已有 wake cycle（活跃周期状态）。

输出（结构化 JSON，schema 校验后落库）：

```json
{
  "conversation_id": "...",
  "analyzed_at": "...",
  "customer_state": "S1-S11 之一",
  "buyer_intent": "…",
  "opportunity_stage": "…",
  "products": [], "quantity": null, "requirements": [],
  "quote_status": "none | requested | preparing | sent | negotiating",
  "buyer_waiting_for": "…", "seller_waiting_for": "…",
  "last_meaningful_sender": "buyer | seller",
  "read_status": {...},
   "substantive_reply": {"is": true, "reason": "…", "message_id": "…"},
   "waiting_phase": {"started": true, "trigger_message_id": "...", "trigger_message_at": "..."},
   "seller_message_class": "followup_wake | substantive_update | courtesy | none",
   "seller_message_class_reason": "…（仅当上一条新消息来自卖家时输出，说明分类依据；none=无卖家新消息）",
   "next_action": "…", "next_action_reason": "…",
  "pause_until": null, "stop_contact": false,
  "wake_cycle_event": "none | start | advance | end",
  "suggested_reply": "…（仅业务员报告展示，不发送）",
  "confidence": 0.0,
  "evidence": [{"message_id": "...", "quote": "…"}],
  "requires_human_confirmation": false
}
```

### 7.3 报价状态识别（不建报价系统）

系统只识别四态：`requested`（客户要报价）/ `preparing`（承诺了在准备）/ `sent`（已发出）/ `negotiating`（已报价在谈判）。输出 `buyer_waiting_for` / `seller_waiting_for` 供报告提示业务员下一步（例如"等内部运费 → 今天该催货代"）。**不计算价格、不接报价系统。**

### 7.4 AI 输出硬约束（违反即拒收）

| # | 约束 |
| --- | --- |
| A1 | 不得编造产品价格、MOQ、运费、DDP 价格、交期、认证、库存、产品参数——无依据时输出 `unknown` / `needs_confirmation` / `waiting_internal` |
| A2 | 不得直接产出 due_at / next_check_at / 次数 / cycle_status（状态机专属） |
| A3 | substantive_reply=false 的礼貌回复，不得据此推进周期 |
| A3' | substantive_reply=true 必须附 message_id 证据 |
| A4 | 置信度 < 0.6 或证据不足 → requires_human_confirmation=true（转 S10） |
| A5 | evidence 必须引用真实 message_id 与原文，禁止无出处的断言 |
| A6 | suggested_reply 不得包含编造的数字/参数，且必须标注"草稿，仅供参考" |
| A6' | suggested_reply 尊重 v1.2 沉淀的多语言经验（西语/希伯来语等按客户背景破冰） |
| A7 | 输出必须过 JSON schema 校验；校验失败整条拒收重试（上限 2 次），仍失败 → 该会话转 S10 |

---

## 8. 报告

### 8.1 老板总报告（每日一份）

| 区块 | 内容 |
| --- | --- |
| 头部 | 日期、运行类型（定时/补跑）、扫描窗口、diff 概览（NEW/CHANGED/重入数） |
| 核心指标 | 最近 30 天活跃客户数、今天需处理客户数、各业务员待处理数 |
| 状态分布 | S1 未回复 / S2 待报价 / S3 报价准备中 / S4 已报价待回复 / S5 今日唤醒 / S11 已结束唤醒 |
| 风险 | 超时未处理（S1 超过响应 SLA、任务已逾期） |
| 高意向 | 高意向客户清单及当前卡点 |
| 待人工 | S10 清单（含原因） |
| 执行情况 | 各业务员昨日报告完成情况（Shadow 期先只统计动作卡勾选） |

### 8.2 业务员行动报告（每人每日一份）

每个客户一张行动卡，直接回答"今天干什么、为什么"：

| 字段 | 说明 |
| --- | --- |
| 客户 / 国家 | 谁 |
| 当前状态 | S 状态 + 等待多久（"等待客户 5 天"） |
| 客户核心需求 | 最近实质内容摘要（含产品/数量，未知的写"未知"） |
| 当前问题 | 阻塞点 |
| 今天应该做什么 | next_action |
| 为什么 | next_action_reason + 证据引用 |
| 推荐回复 | suggested_reply（标注"仅供参考，不自动发送"） |
| 唤醒轮次 | "第 2 轮唤醒（共 4 轮）" |
| 下次检查日期 | next_check_at |

### 8.3 交付与分发

Shadow Mode 期：报告写入本地 + 呈报管理员（老板）审核，不外发。稳定后接入飞书/微信按 owner 分发（渠道：现有 Channel 授权：飞书 1 群 + 1 用户、微信 my_account）。

---

## 9. Shadow Mode

### 9.1 定义

每天真实抓取、真实分析、真实出报告，但：

- 不自动联系客户（本来也不允许）；
- 不直接改变人工业务结果；
- 报告只给管理员（老板）。

### 9.2 验证维度（每项都要有 Shadow 期统计口径）

漏报（该处理没进报告）、误报（不该处理进了报告）、商机状态错误、报价状态错误、业务员未回复误判、实质回复误判、唤醒周期边界错误、推荐回复质量（人工评分）、owner 分配错误。

### 9.3 稳定门槛

连续 **10 个工作日**：漏报 = 0（红线）、误报率 < 5%、状态错误率 < 3%、周期边界错误 = 0（红线）→ 经老板确认后开放正式分发。

---

## 10. 工程要求：开发顺序

### 必须先做（阶段 1）

1. v1.3 浏览器真实数据层（列表 + 详情 + 消息级抓取，§4 契约）；
2. 稳定 buyer_id（§4.2 调查）；
3. conversation/message 标识；
4. 已读未读（或确认不可行，D7）；
5. 更新时间语义（Marina 调查，D3）；
6. 30 天增量扫描 + checkpoint（§5）；
7. wake cycle 显式周期模型（§3 schema + 状态机，含测试）。

### 随后做（阶段 2）

AI Analyzer（§7）→ 长期商机记忆落库（§6）→ 老板报告 → 业务员报告 → 接推荐回复 → Shadow Mode 跑通。

### 稳定后再做（阶段 3）

Accio Work 定时执行 + 开机补跑；飞书/微信正式分发。

### 暂时不做

自动发送客户消息、自动报价、报价系统、大型 UI、复杂 CRM。

---

## 11. 待老板拍板事项（Open Decisions）

| # | 事项 | 选项与建议 |
| --- | --- | --- |
| D1 | ~~业务员在唤醒已执行后又主动发实质新消息，计为唤醒事件还是结束旧周期？~~ **已由老板审订解决（2026-09-17）**：不再采用单一规则，改为**三分类**（§3.3）——`followup_wake` 占用当前轮次；`substantive_update` 结束旧 waiting phase、重建锚点、重新从 3 天起算；`courtesy` 不动锚点不占轮次。剩余开放点仅为 AI 分类判定的具体阈值（阶段 2） | 分类实现细节在阶段 2 AI Analyzer 开发时验证；本阶段数据结构已按三分类定义 |
| D2 | v1.2 的"90 天 6 次客户级限次"是否保留为观察指标？ | 建议**降级为报告指标**（不拦截、只统计），因为新模型单周期已含 4 次，跨周期频率暂无证据需要硬限 |
| D3 | 列表 update_time 语义 | 不是决策，是调查项（§4.3）；结论出来后需老板确认"可否用于沉默推导" |
| D4 | FINAL WAKE 后周期立即 ended，还是再等一段观察期？ | 已由老板审订澄清：ended 的触发以**实际发送确认**为前提（FINAL WAKE `status=sent` 后仍无实质回复 → 立即 ended）；仅 reported 不得 ended。是否追加 +3 天观察窗仍可讨论，默认**不追加**（规则原文如此） |
| D5 | S1（业务员未回复）的响应 SLA 时长（多少小时算"超时未处理"） | 建议 24h（北京时间）；需老板定 |
| D6 | 4 个 history_unknown 候选的处置 | 建议：保持 S10，在其产生新消息后按新模型建周期；**不建议**用旧 contacts 猜测历史 |
| D7 | 买家已读回执若平台不提供，是否接受"不可行"结论 | 建议：接受并从契约移除，报告改用"已发送 N 天"口径 |
| D8 | Shadow 期报告审核的交付形式（本地文件 / 每日呈报 / 飞书发老板） | 建议：本地文件 + 每日呈报，避免 Shadow 期就把报告发进群 |

---

## 12. 验收标准

### 12.1 浏览器数据层

同一客户连续多次扫描（≥3 次，跨 ≥2 天）：

- buyer_id 稳定（同一 buyer 两次扫描值一致）；
- owner 稳定（无人工改派时不变）；
- message 时间正确（与页面展示一致，误差 0）；
- sender 正确（买家/卖家/机器人不混淆）；
- read status 正确（若 D7 判定可行）。

### 12.2 增量扫描

- 连续运行两次、无数据变化：第二次 UNCHANGED 会话 **0 次深度分析**；
- 新增一条买家消息：该会话必须 CHANGED；
- 停机一天后补跑：重入集正确（到期任务/唤醒轮次/约定日期不遗漏）。

### 12.3 唤醒周期（完整链路 + 边界）

时序测试（模拟时钟推进）：

| 步骤 | 期望 |
| --- | --- |
| 卖家有效消息 T，无回复 3 天 | WAKE 1 到期（status=due）；入报告 → reported；**未实际发送前 cycle_status 不变** |
| WAKE 1 对应跟进消息实际发出 | 该轮 status=sent、actual_sent_at=消息时间、source_message_id 落定 → cycle_status=wake1_sent；WAKE 2 due_at = actual_sent_at+5 |
| WAKE 1 reported 但业务员未发送、又过 5 天 | 周期不推进；报告持续重提该轮；不生成 WAKE 2 due_at |
| +5 天（自 actual_sent_at）无回复，跟进消息实际发出 | WAKE 2 sent → wake2_sent |
| +5 天（自 actual_sent_at）无回复，跟进消息实际发出 | WAKE 3 sent → wake3_sent |
| 从 W1 actual_sent_at 起第 30 天，跟进消息实际发出 | FINAL WAKE sent → final_sent → **ended 仅在实际发送确认后落定** |
| FINAL WAKE 仅 reported 未发送 | 周期不得置 ended；持续重提 |
| 再无回复 | 不再产生新唤醒任务（第 5 次唤醒不存在） |
| 任意阶段买家实质回复 | 旧 cycle 立即 ended（end_reason=substantive_reply），未发送轮次置 cancelled，重分析，计数不沿用 |
| 连续 3 条卖家消息（同等待阶段、courtesy 或未成周期前） | **只产生 1 个周期**，锚点=最后一条有效消息 |
| 周期中途卖家发 `followup_wake` 类消息 | 占用当前 due/reported 轮次：status=sent、actual_sent_at=真实消息时间；不新建周期 |
| 周期中途卖家发 `substantive_update` 类消息 | 旧周期 ended（未发送轮次 cancelled）；新锚点=该消息；重新从 3 天起算 |
| 周期中途卖家发 `courtesy` 类消息 | 锚点不动、轮次不占、wake_events 不变 |
| 长期停机后补跑（中间轮次已晚于 FINAL 日） | 只保留最紧急一轮（直接推进 FINAL WAKE），不连续补多条 |

### 12.4 幂等性

相同数据重复运行 N 次（含补跑场景）：

- 任务不重复创建（task_id 去重）；
- 周期不重复创建（trigger_message_id 去重）；
- 唤醒轮次不重复推进（`sent` 事件按 source_message_id 去重；同一消息只回填一次 actual_sent_at）；
- ContactEvent 不重复累计（event_id 去重）；
- 当日运行以 `run_status == success` 收口；`partial` / `failed` 修复后可再次补跑，重复补跑不产生重复实体（checkpoint 判定）。

### 12.5 报告

- 老板报告指标与记忆实体计数一致（抽查 3 个数字可追溯到实体）；
- 业务员报告每张卡字段齐全（§8.2 表）；
- 推荐回复全部带"仅供参考"标注，无自动发送路径（代码层面不存在发送调用）。

### 12.6 AI 约束

- 编造事实测试（注入无价格依据的会话）：输出必须含 unknown/needs_confirmation，不得出现编造数字；
- A2 测试：Analyzer 原始输出即使包含 due_at 字段也必须被状态机忽略/拒收。

---

# v1.3 推荐开发顺序

> **完成本 MD 后，下一步只执行第一阶段。不要一次性开发全部 v1.3。**

### 阶段 1：数据地基（只做这一步）

**目标**：不动业务逻辑，把"眼睛"装好——能稳定、幂等、低消耗地拿到可信数据。

| 步骤 | 内容 | 产出 |
| --- | --- | --- |
| 1.1 | 消息中心列表抓取器（30 天窗口、分页、XHR 优先）+ 数据契约字段落库 | `capture_list.py` + `state/` 新结构（Customer/Opportunity/Conversation 初版） |
| 1.2 | buyer_id 调查（§4.2 五条路线逐一验证）+ conversation_id/message_id 确定 | 调查记录 + 字段值入契约 |
| 1.3 | 详情页抓取器（消息级：id/时间/sender/kind/文本） | `capture_detail.py` |
| 1.4 | 已读未读可行性（D7）+ Marina 时间语义（D3） | 结论记录，不可行字段从契约移除 |
| 1.5 | 增量扫描 + checkpoint（NEW/CHANGED/UNCHANGED + 重入集） | `scanner.py` + checkpoint 落盘 |
| 1.6 | wake cycle 周期模型（schema + 状态机 + §12.3/12.4 测试） | `cycle_engine.py` + 测试全绿 |

**阶段 1 验收** = §12.1 + §12.2 + §12.3 + §12.4 全部通过 + 消耗实测记录（L1/L2 零 LLM）。

### 阶段 2：大脑（数据稳定后）

AI Analyzer 契约与校验器 → 记忆实体完整落库（含迁移脚本）→ 老板报告 → 业务员报告 → 推荐回复 → Shadow Mode 日常化。

### 阶段 3：上线（Shadow 稳定后）

Accio Work 定时 + 开机补跑 → 飞书/微信正式分发。

---

## 附录 A：现有代码与 v1.3 新规则的冲突清单

| # | 冲突点 | 位置 | 处置 |
| --- | --- | --- | --- |
| C1 | 旧唤醒规则（3 次上限/5-7 工作日/30 冷却/90 天 6 次）与新 3/5/5/30 自然日规则根本不同 | `wake_rules.py` 全部决策树 | 阶段 1.6 重写为 cycle_engine；旧 decide_wake 保留到测试迁移完 |
| C2 | 计数从 contacts 派生的隐患 | `cycle_wake_count()` | v1.3 周期模型显式记录 wake_events，彻底消除派生 |
| C3 | buyer 姓名聚合的临时键 | `aggregate_buyer_contacts` | buyer_id 调查（1.2）后替换 |
| C4 | 列表 update_time 被当作会话时间用 | seed 数据中 update_time 语义混杂 | D3 调查；契约中原样保存不推导 |
| C5 | state 扁平结构（opportunities/tasks/wakes）vs 新实体模型 | state/*.json | 迁移脚本（§6.3），旧目录只读备份 |
| C6 | `assignee_roles.json` 硬编码 sales 列表 vs owner 应从列表实抓 | capture 层 | owner 从列表抓取；roles 文件降级为别名/顺序配置 |
| C7 | 现有测试直接构造 wakes dict（无周期实体） | test_engine 11/12 组 | 随 cycle_engine 新测试体系迁移，旧断言改写为周期语义 |
| C8 | daily_run 的 batch 由人工构造 | `daily_run.py` demo | 阶段 2 由捕获器+Analyzer 自动产出 |
| C9 | `history_unknown` 语义在新模型无对应 | wakes.json 4 条 | S10 替代（D6） |
| C10 | 工作日/节假日历概念 | wake_rules 全局 | 废弃（自然日） |
| C11 | 日报单一读者（老板） | generate_report | 拆双报告渲染器（老板/业务员） |
| C12 | v1.2 任务模型无 S2/S4/S5/S6/S11 对应任务类型 | tasks action_type 体系 | 扩展任务类型表，S 状态与任务类型解耦 |

---

## 附录 B：本规格未覆盖、需在阶段 1 中一并调查的事项

1. 商机沟通列表的 XHR 分页游标格式（决定扫描器实现方式）；
2. 30 天窗口在列表页的实际可达深度（按"最后联系时间"筛选是否支持、分页上限）；
3. TM 会话与询盘会话在同一 conversation_id 体系下的关系（谈单助手样本显示两渠道并存）；
4. 消息 kind 分类（机器人/系统/人工）在 DOM/XHR 中的可辨识字段；
5. Accio Work Skill 内嵌浏览器与现有隔离目录方案的衔接方式。

