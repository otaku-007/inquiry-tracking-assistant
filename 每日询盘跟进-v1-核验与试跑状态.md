# 每日询盘跟进 v1 —— 核验与试跑状态（最终版）

> 更新时间：2026-09-11 17:55（Asia/Shanghai）
> 状态：**已完成一次真实只读试跑**（浏览器子代理路径，见第三节）。

---

## 一、已验证能力（真实核验，非承诺）

### 1. 登录与数据访问
- ✅ 已登录阿里国际站，登录态有效，未跳 `login.alibaba.com`。
- ✅ 消息中心「全部」共 **96 页 ≈ 1920 条询盘**，列表可翻页。
- ✅ 列表字段可读：询价单号、买家名、来源（Inquiry from TM / Product Details Page）、分类（TM 商机 / 询盘商机）、负责人、更新时间、创建时间、国家、高意向标。
- ✅ 详情页可读：消息时间戳、发言方（buyer/seller/bot）、原文。

### 2. 「询盘统计」skill 抓取链路（代码级核验）
- 统一入口 `scripts/accio_scrape_manager.py start/wait/finish`，`run_scheduled.py` 定时入口。
- `--mode new`（按 create_time 抓当日新建）+ `--start-time/--end-time`（历史窗）+ `--category/--assignee/--buyer-name` 过滤。
- `--depth full` 滚动拉全量历史消息，含 `speaker/timestamp/text`；`inquiries.json` 字段结构完整（inquiry_id、assignee、detail.messages、detail.stats 等）。

### 3. 本轮真实试跑（已完成）
- 用**浏览器子代理**进入列表第 1 页（20 条）＋ 今日有更新的 12 条详情页，逐条判定消息方向，产出《每日询盘跟进日报-试跑-2026-09-11.md》。

---

## 二、覆盖缺口（须在正式版处理）

1. **Chrome CDP 端口**：`--remote-debugging-port=9222` 在「全新 user-data-dir」下可绑定，但在「默认目录 + 既有 Profile 2」下**始终不绑定**（已排除引号、单例交接、崩溃弹窗、企业策略）。→ 本轮改用**浏览器子代理**完成试跑；要让 `询盘统计` skill 的 Playwright 脚本直连，仍需另解 CDP（见第五节）。
2. **老客户新消息**：`--mode new` 只抓当日新建；历史会话新消息需「历史窗 + 按 `detail.messages[].timestamp` 本地筛」。列表页**没有独立的"最后消息时间"**，只有更新时间/创建时间 → 必须进详情页判定。
3. **买家等级 L0–L4**：列表页只有统一图标、**无 L0–L4 文字**，无法自动筛「重点买家」→ 需详情页/后台另取或人工标注。
4. **内部任务阻塞**：聊天记录看不到内部状态（等样品/工厂交期/财务），需 OKKI/CRM 或人工标注，否则只能标「待核查」。
5. **重复线索**：同一买家多条询盘 ID 可命中同一会话（试跑中 NSABIMANA ×3、Hashim ×2），需去重防止重复联系。
6. **bot 自动接待**：部分线程含 bot 消息（试跑中 YANIS 线程），判定"人工回复"时须排除 bot。

---

## 三、Chrome CDP 问题诊断结论

- 现象：Chrome 152.0.7977.83 下，全新目录可绑 9222，默认目录（含 Ben/Profile 2 登录态）不绑、日志无 DevTools 服务启动记录。
- 已排除：参数引号、单例交接（确认干净直接启动）、`--disable-session-crashed-bubble`、资料选择页、企业策略（注册表无）。
- **可行替代**：浏览器子代理（`agent_id=browser`）能驱动用户 Chrome、保留登录态、完成只读抓取——本轮试跑即走此路径。
- 待办：若要 `询盘统计` skill 自身脚本直连 9222，需继续排查 Chrome 152 行为（可能是版本对既有 profile + 调试端口的特定问题）。

---

## 四、规则配置（来自附件《讨论稿》，已收到并落地）

- 商机状态：活跃推进 / 等待客户 / 等待内部 / 约定暂停 / 待激活候选 / 激活尝试中 / 低频观察 / 成交 / 已关闭。
- 行动类型：待回复 / 承诺待兑现 / 高意向待推进 / 报价样品项目节点待处理 / 待激活 / 待核查（可并存）。
- 待激活 6 条件、默认沉默窗口 5 团队工作日。
- 唤醒限制：每周期最多 3 次；第 2 次距第 1 次 ≥5 工作日、第 3 次距第 2 次 ≥7 工作日；第 3 次后暂停 ≥30 自然日；同一买家 90 天滚动最多 6 次。
- 计数规则：只计实际已发送联系；换负责人/平台/重跑不清零。
- **实现状态**：本轮试跑仅完成"状态/行动类型判定（含聊天证据）"；**唤醒次数持久化、13 条模拟用例、定时/投递尚未实现**（需先确认接收人/渠道/时间）。

---

## 五、真实消耗记录

- **`询盘统计` skill 抓取/摘要 LLM 消耗：0**（CDP 未开，脚本未跑，未产生任何抓取或摘要）。
- **浏览器子代理**：本轮共 2 次只读任务（列表页 + 12 条详情页），产生 token 消耗，但**无精确 token 计数**，如实标记"未计量"。
- **确定性规则**（去重、消息方向、状态判定）零 LLM。
- 结论：不承诺"低消耗"；真实 token 数需正式运行后按实际记录。

---

## 六、最少待确认项

1. 是否接受「浏览器子代理」作为抓取通道，还是必须修通 Chrome CDP 9222（让 skill 脚本直连）。
2. 正式版是否需要**全量扫描 96 页**（本轮仅第 1 页）——决定每日成本与时长。
3. Alan Au 今日覆盖为 0（第 1 页未见），需确认其询盘是否在后续页、或当前确实无新增。
4. 买家等级 L0–L4 字段来源（详情页/后台/人工标注）。
5. 日报接收人、渠道（微信具体群/人）、发送时间——确认后再启用定时与内部投递。
6. 唤醒次数持久化存储位置 + 13 条模拟用例（用于验收规则）。
