# v1.2 交付与验证结果

日期：2026-09-12。状态：**已完成代码修复、统一唤醒资格判断、动态日报入口与模拟验证；未做任何自动联系或推送，未扩大抽样。**

---

## 一、本次修复（对应独立复核 6 项）

| # | 复核问题 | 修复 | 验证 |
| --- | --- | --- | --- |
| 1 | 重跑初始导入线索 40→20 退回 | 商机改为按 `lead_id` 幂等合并（`upsert_opportunity`），不再整体替换；样本回放与日常运行分开 | 测试 7a/7b |
| 2 | 人工延期日期/动作被旧数据覆盖 | `upsert_task` 新增 `MANUAL_FIELDS` 保护 + `manual_override` 标记；`new_signal=True` 才允许覆盖并清除人工标记 | 测试 8a–8d |
| 3 | 延期任务到期不进入日报 | `is_review_due` 统一判断：open 恒可见；deferred/waiting 复查时间到后重新可见；日报新增「到期复查队列」 | 测试 9a–9c |
| 4 | 历史唤醒候选不进入日报 | 日报读取 `wakes`，经统一资格判断分「今天可联系」与「候选待核实」 | 见第三节/日报 |
| 5 | 报告日期/范围/计数固定 | `generate_report` 全部参数化：as_of、cutoff、coverage 来自本次运行；计数从实际 state 计算；角色路由读 `assignee_roles.json` | 见日报 |
| 6 | 状态读取失败被空默认值覆盖 | `_load` 区分「不存在」与「损坏」；损坏时改名保留原文件并抛 `StateCorruptError`；`save_section` 拒绝保存 None | 测试 10a–10e |

**备份**：修改前已将 state/ 全量复制到 `state/backups/2026-09-12T121555/`。日常运行入口 `daily_run.run_daily` 在每次运行前自动再备份一次。

---

## 二、统一唤醒资格判断（wake_rules.py）

单一决策入口 `decide_wake()`，输出统一结果：**可联系 / 等待至某日 / 待核实 / 停止联系**，每项带原因与下次检查时间。试行配置（`WAKE_CONFIG`）：

| 规则 | 值 |
| --- | --- |
| 每周期无实质回复唤醒上限 | 3 次 |
| 第 2 次距第 1 次 | ≥5 团队工作日 |
| 第 3 次距第 2 次 | ≥7 团队工作日 |
| 第 3 次后冷却 | ≥30 自然日（到期不自动重开，需新事实/新方案） |
| 同一买家 90 天滚动 | ≤6 次（跨项目/账号/渠道合并） |
| 默认沉默窗口 | 5 团队工作日 |

判定优先级：停止联系 > 历史次数未知(→待核实) > 无有效需求(→待核实) > 约定暂停(未到→等待/到期→复查) > 冷却期 > 周期上限与工作日间隔 > 90 天滚动上限 > 供应价值(无→待核实) > 可联系。

关键边界（已实现并验证）：
- **联系记账与允许联系分开**：`contacts` 永久保留真实历史，限次只影响「是否可联系」，不丢弃已发生记录。
- **历史次数未知不默认为零**：`history_unknown=True` 一律 `待核实`，不进入可联系队列。
- **客户级跨询盘限次**：仅当存在稳定 `buyer_id` 时才可信聚合；当前数据只有姓名，按姓名聚合不宣称已实现客户级限次（见「未验证限制」）。

验证：测试 11a–11k 覆盖三次上限、两次工作日间隔、冷却期、90 天滚动、未知历史、无有效需求、停止联系、无供应方案。

---

## 三、约定暂停：模拟原始聊天完整闭环（测试 12a–12c）

未手写 `classification=暂停`，而是从模拟原始聊天走完整链路：

1. 原始聊天：买家「Thanks, I'll contact you around Oct 5 for the hotel project order.」
2. 分析提取 `pause_until=2026-10-05`。
3. 到期前（09-12）判定 → **等待至某日**，不进入可联系。
4. 到期日（10-05）判定 → **待核实（进入复查，不自动联系）**。
5. 客户后续主动回复 → `apply_substantive_reply` 重置周期并重判为 **可联系**。

真实「约定下月联系」样本：**暂未补找**（遵循「暂不扩大抽样」）。已给出受限补找方案：最多 10 个历史会话详情，记录范围与消耗；找不到则保留真实样本缺口，不无限扫描凑案例。该步需浏览器会话，列入下一轮待办。

---

## 四、Marina 列表时间与详情时间不一致

- 列表 `update_time` = **2025-09-08**；详情最后消息 = **2026-04-26 03:34**。
- 结论：两者语义不同（或存在提取错误）。**列表「更新时间」不随聊天消息变化**，很可能是询盘资料/跟进状态/系统更新时间，不是「最后聊天时间」。
- 影响：不能用列表 update_time 证明「一年没聊天」，也不能据此排除后续页新消息；不能声称实时/完整增量覆盖。
- 处理：已在 `wakes.json` 的 Marina 记录写入 `time_discrepancy_note`，并在日报「仍未验证的限制」标注。字段真实语义需回查后台，或改用可核验的会话更新时间来源（详情轮查并明确发现延迟）。

---

## 五、1912 表述修正

- 旧表述「已扫描 1912 条」错误。
- 正确区分：**后台显示总线索 ≈ 1912 条（96 页）**（仅为显示总数）；**实际读取** 第 1、95、96 页共 3 页；**去重后记录数** 40；**详情复核数** 16（第 1 页 12 + 历史 4）。
- 已写入 `state/checkpoints.json` 的 coverage 与日报「运行统计与覆盖缺口」。

---

## 六、日常运行入口（daily_run.py）与样本回放分开

- **日常运行**：`daily_run.run_daily(batch)`，输入 = 本次新抓取 + 分析产物（leads / tasks / events / wake_contacts）。流程：备份 → 安全读取 → 合并线索 → 合并任务（含 new_signal 重开）→ 应用事件 → 刷新到期 → 记检查点 → 保存（跳过读取失败的 section）→ 生成日报。**不整体替换商机库，不改种子代码。**
- **样本回放**：`seed_state.py`（首屏 20 条）与 `seed_silent.py`（历史沉默 20 条）仅用于回放已核验的真实材料，现也改为幂等合并。
- **演示**：`daily_run.demo()` 在真实状态的临时副本上跑通「新线索 → 待回复任务」「旧会话新回复重开」「约定暂停事件」，产出 `每日询盘跟进日报-演示-2026-09-12.md`。真实状态不受污染（仍 40 线索/12 任务/4 唤醒）。

---

## 七、可重复验证结果

`python test_engine.py` 共 **40 项全部 PASS**：

- v1.1 原有 12 项：幂等重跑、跨日到期、联系去重、回复暂停、人工已处理保留、部分失败保留。
- v1.2 新增：导入后重跑不丢失（40 不退 20）、延期日期/动作/理由保留、延期到期重新可见、损坏状态保护、统一唤醒资格 11 项、约定暂停模拟闭环 3 项。

真实日报由实际 state 生成：`每日询盘跟进日报-2026-09-12.md`（40 线索 / 37 会话 / 35 客户 / 9 待办 / 4 待核实候选 / 0 今天可联系）。

---

## 八、仍未验证的限制

1. **客户级 90 天滚动限次**：需稳定 `buyer_id` 聚合；当前只有姓名，不能宣称已实现。
2. **列表「更新时间」字段语义**：Marina 案例未回查，暂不能作为「最后聊天时间」。
3. **真实「约定暂停」样本**：已模拟验证，真实样本未补找（受限方案：≤10 个历史详情）。
4. **覆盖缺口**：仅抽样 3 页，未全量扫描；无法发现清单外新客户。
5. **节假日历、团队时区、日报接收人/渠道、运行时间**：均未配置，暂不启用推送。
6. **真实消耗**：代码级零 LLM（确定性规则）；浏览器抓取与模型摘要的 token/积分未计量，不承诺低消耗。
7. **工作日历**：当前按周一~周五，未接入法定节假日。

---

## 九、下一轮待办

- 回查列表「更新时间」字段真实语义（或改用详情轮查）。
- 有限补找真实「约定暂停」样本（≤10 个历史详情，记录范围与消耗）。
- 确认 buyer_id 来源后，实现可信的客户级 90 天滚动聚合。
- 确认接收人/渠道/运行时间后，再接入定时与投递（当前仅内部日报，不自动联系客户）。
